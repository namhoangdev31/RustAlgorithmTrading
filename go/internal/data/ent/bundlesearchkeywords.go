// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"trading/control-gateway/internal/data/ent/bundles"
	"trading/control-gateway/internal/data/ent/bundlesearchkeywords"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// BundleSearchKeywords is the model entity for the BundleSearchKeywords schema.
type BundleSearchKeywords struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// BundleId holds the value of the "bundleId" field.
	BundleId uuid.UUID `json:"bundleId,omitempty"`
	// Keyword holds the value of the "keyword" field.
	Keyword string `json:"keyword,omitempty"`
	// Locale holds the value of the "locale" field.
	Locale string `json:"locale,omitempty"`
	// Weight holds the value of the "weight" field.
	Weight int `json:"weight,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BundleSearchKeywordsQuery when eager-loading is set.
	Edges        BundleSearchKeywordsEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BundleSearchKeywordsEdges holds the relations/edges for other nodes in the graph.
type BundleSearchKeywordsEdges struct {
	// Bundle holds the value of the bundle edge.
	Bundle *Bundles `json:"bundle,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BundleOrErr returns the Bundle value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BundleSearchKeywordsEdges) BundleOrErr() (*Bundles, error) {
	if e.Bundle != nil {
		return e.Bundle, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: bundles.Label}
	}
	return nil, &NotLoadedError{edge: "bundle"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BundleSearchKeywords) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bundlesearchkeywords.FieldWeight:
			values[i] = new(sql.NullInt64)
		case bundlesearchkeywords.FieldKeyword, bundlesearchkeywords.FieldLocale:
			values[i] = new(sql.NullString)
		case bundlesearchkeywords.FieldID, bundlesearchkeywords.FieldBundleId:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BundleSearchKeywords fields.
func (_m *BundleSearchKeywords) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bundlesearchkeywords.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case bundlesearchkeywords.FieldBundleId:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field bundleId", values[i])
			} else if value != nil {
				_m.BundleId = *value
			}
		case bundlesearchkeywords.FieldKeyword:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field keyword", values[i])
			} else if value.Valid {
				_m.Keyword = value.String
			}
		case bundlesearchkeywords.FieldLocale:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field locale", values[i])
			} else if value.Valid {
				_m.Locale = value.String
			}
		case bundlesearchkeywords.FieldWeight:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field weight", values[i])
			} else if value.Valid {
				_m.Weight = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BundleSearchKeywords.
// This includes values selected through modifiers, order, etc.
func (_m *BundleSearchKeywords) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBundle queries the "bundle" edge of the BundleSearchKeywords entity.
func (_m *BundleSearchKeywords) QueryBundle() *BundlesQuery {
	return NewBundleSearchKeywordsClient(_m.config).QueryBundle(_m)
}

// Update returns a builder for updating this BundleSearchKeywords.
// Note that you need to call BundleSearchKeywords.Unwrap() before calling this method if this BundleSearchKeywords
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BundleSearchKeywords) Update() *BundleSearchKeywordsUpdateOne {
	return NewBundleSearchKeywordsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BundleSearchKeywords entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BundleSearchKeywords) Unwrap() *BundleSearchKeywords {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BundleSearchKeywords is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BundleSearchKeywords) String() string {
	var builder strings.Builder
	builder.WriteString("BundleSearchKeywords(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("bundleId=")
	builder.WriteString(fmt.Sprintf("%v", _m.BundleId))
	builder.WriteString(", ")
	builder.WriteString("keyword=")
	builder.WriteString(_m.Keyword)
	builder.WriteString(", ")
	builder.WriteString("locale=")
	builder.WriteString(_m.Locale)
	builder.WriteString(", ")
	builder.WriteString("weight=")
	builder.WriteString(fmt.Sprintf("%v", _m.Weight))
	builder.WriteByte(')')
	return builder.String()
}

// BundleSearchKeywordsSlice is a parsable slice of BundleSearchKeywords.
type BundleSearchKeywordsSlice []*BundleSearchKeywords
