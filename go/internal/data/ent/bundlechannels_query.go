// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"trading/control-gateway/internal/data/ent/bundlechannels"
	"trading/control-gateway/internal/data/ent/bundledeliveryrollouts"
	"trading/control-gateway/internal/data/ent/bundlereleases"
	"trading/control-gateway/internal/data/ent/bundles"
	"trading/control-gateway/internal/data/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// BundleChannelsQuery is the builder for querying BundleChannels entities.
type BundleChannelsQuery struct {
	config
	ctx                *QueryContext
	order              []bundlechannels.OrderOption
	inters             []Interceptor
	predicates         []predicate.BundleChannels
	withBundle         *BundlesQuery
	withCurrentRelease *BundleReleasesQuery
	withReleases       *BundleReleasesQuery
	withRollouts       *BundleDeliveryRolloutsQuery
	modifiers          []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BundleChannelsQuery builder.
func (_q *BundleChannelsQuery) Where(ps ...predicate.BundleChannels) *BundleChannelsQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *BundleChannelsQuery) Limit(limit int) *BundleChannelsQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *BundleChannelsQuery) Offset(offset int) *BundleChannelsQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *BundleChannelsQuery) Unique(unique bool) *BundleChannelsQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *BundleChannelsQuery) Order(o ...bundlechannels.OrderOption) *BundleChannelsQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryBundle chains the current query on the "bundle" edge.
func (_q *BundleChannelsQuery) QueryBundle() *BundlesQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlechannels.Table, bundlechannels.FieldID, selector),
			sqlgraph.To(bundles.Table, bundles.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlechannels.BundleTable, bundlechannels.BundleColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCurrentRelease chains the current query on the "currentRelease" edge.
func (_q *BundleChannelsQuery) QueryCurrentRelease() *BundleReleasesQuery {
	query := (&BundleReleasesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlechannels.Table, bundlechannels.FieldID, selector),
			sqlgraph.To(bundlereleases.Table, bundlereleases.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlechannels.CurrentReleaseTable, bundlechannels.CurrentReleaseColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReleases chains the current query on the "releases" edge.
func (_q *BundleChannelsQuery) QueryReleases() *BundleReleasesQuery {
	query := (&BundleReleasesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlechannels.Table, bundlechannels.FieldID, selector),
			sqlgraph.To(bundlereleases.Table, bundlereleases.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlechannels.ReleasesTable, bundlechannels.ReleasesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRollouts chains the current query on the "rollouts" edge.
func (_q *BundleChannelsQuery) QueryRollouts() *BundleDeliveryRolloutsQuery {
	query := (&BundleDeliveryRolloutsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlechannels.Table, bundlechannels.FieldID, selector),
			sqlgraph.To(bundledeliveryrollouts.Table, bundledeliveryrollouts.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlechannels.RolloutsTable, bundlechannels.RolloutsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BundleChannels entity from the query.
// Returns a *NotFoundError when no BundleChannels was found.
func (_q *BundleChannelsQuery) First(ctx context.Context) (*BundleChannels, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bundlechannels.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *BundleChannelsQuery) FirstX(ctx context.Context) *BundleChannels {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BundleChannels ID from the query.
// Returns a *NotFoundError when no BundleChannels ID was found.
func (_q *BundleChannelsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bundlechannels.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *BundleChannelsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BundleChannels entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BundleChannels entity is found.
// Returns a *NotFoundError when no BundleChannels entities are found.
func (_q *BundleChannelsQuery) Only(ctx context.Context) (*BundleChannels, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bundlechannels.Label}
	default:
		return nil, &NotSingularError{bundlechannels.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *BundleChannelsQuery) OnlyX(ctx context.Context) *BundleChannels {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BundleChannels ID in the query.
// Returns a *NotSingularError when more than one BundleChannels ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *BundleChannelsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bundlechannels.Label}
	default:
		err = &NotSingularError{bundlechannels.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *BundleChannelsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BundleChannelsSlice.
func (_q *BundleChannelsQuery) All(ctx context.Context) ([]*BundleChannels, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BundleChannels, *BundleChannelsQuery]()
	return withInterceptors[[]*BundleChannels](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *BundleChannelsQuery) AllX(ctx context.Context) []*BundleChannels {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BundleChannels IDs.
func (_q *BundleChannelsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(bundlechannels.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *BundleChannelsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *BundleChannelsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*BundleChannelsQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *BundleChannelsQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *BundleChannelsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *BundleChannelsQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BundleChannelsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *BundleChannelsQuery) Clone() *BundleChannelsQuery {
	if _q == nil {
		return nil
	}
	return &BundleChannelsQuery{
		config:             _q.config,
		ctx:                _q.ctx.Clone(),
		order:              append([]bundlechannels.OrderOption{}, _q.order...),
		inters:             append([]Interceptor{}, _q.inters...),
		predicates:         append([]predicate.BundleChannels{}, _q.predicates...),
		withBundle:         _q.withBundle.Clone(),
		withCurrentRelease: _q.withCurrentRelease.Clone(),
		withReleases:       _q.withReleases.Clone(),
		withRollouts:       _q.withRollouts.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithBundle tells the query-builder to eager-load the nodes that are connected to
// the "bundle" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleChannelsQuery) WithBundle(opts ...func(*BundlesQuery)) *BundleChannelsQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBundle = query
	return _q
}

// WithCurrentRelease tells the query-builder to eager-load the nodes that are connected to
// the "currentRelease" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleChannelsQuery) WithCurrentRelease(opts ...func(*BundleReleasesQuery)) *BundleChannelsQuery {
	query := (&BundleReleasesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCurrentRelease = query
	return _q
}

// WithReleases tells the query-builder to eager-load the nodes that are connected to
// the "releases" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleChannelsQuery) WithReleases(opts ...func(*BundleReleasesQuery)) *BundleChannelsQuery {
	query := (&BundleReleasesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withReleases = query
	return _q
}

// WithRollouts tells the query-builder to eager-load the nodes that are connected to
// the "rollouts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleChannelsQuery) WithRollouts(opts ...func(*BundleDeliveryRolloutsQuery)) *BundleChannelsQuery {
	query := (&BundleDeliveryRolloutsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRollouts = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BundleId uuid.UUID `json:"bundleId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BundleChannels.Query().
//		GroupBy(bundlechannels.FieldBundleId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *BundleChannelsQuery) GroupBy(field string, fields ...string) *BundleChannelsGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BundleChannelsGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = bundlechannels.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BundleId uuid.UUID `json:"bundleId,omitempty"`
//	}
//
//	client.BundleChannels.Query().
//		Select(bundlechannels.FieldBundleId).
//		Scan(ctx, &v)
func (_q *BundleChannelsQuery) Select(fields ...string) *BundleChannelsSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &BundleChannelsSelect{BundleChannelsQuery: _q}
	sbuild.label = bundlechannels.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BundleChannelsSelect configured with the given aggregations.
func (_q *BundleChannelsQuery) Aggregate(fns ...AggregateFunc) *BundleChannelsSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *BundleChannelsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !bundlechannels.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *BundleChannelsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BundleChannels, error) {
	var (
		nodes       = []*BundleChannels{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withBundle != nil,
			_q.withCurrentRelease != nil,
			_q.withReleases != nil,
			_q.withRollouts != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BundleChannels).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BundleChannels{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withBundle; query != nil {
		if err := _q.loadBundle(ctx, query, nodes, nil,
			func(n *BundleChannels, e *Bundles) { n.Edges.Bundle = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCurrentRelease; query != nil {
		if err := _q.loadCurrentRelease(ctx, query, nodes, nil,
			func(n *BundleChannels, e *BundleReleases) { n.Edges.CurrentRelease = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withReleases; query != nil {
		if err := _q.loadReleases(ctx, query, nodes,
			func(n *BundleChannels) { n.Edges.Releases = []*BundleReleases{} },
			func(n *BundleChannels, e *BundleReleases) { n.Edges.Releases = append(n.Edges.Releases, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRollouts; query != nil {
		if err := _q.loadRollouts(ctx, query, nodes,
			func(n *BundleChannels) { n.Edges.Rollouts = []*BundleDeliveryRollouts{} },
			func(n *BundleChannels, e *BundleDeliveryRollouts) { n.Edges.Rollouts = append(n.Edges.Rollouts, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *BundleChannelsQuery) loadBundle(ctx context.Context, query *BundlesQuery, nodes []*BundleChannels, init func(*BundleChannels), assign func(*BundleChannels, *Bundles)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleChannels)
	for i := range nodes {
		fk := nodes[i].BundleId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundles.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "bundleId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BundleChannelsQuery) loadCurrentRelease(ctx context.Context, query *BundleReleasesQuery, nodes []*BundleChannels, init func(*BundleChannels), assign func(*BundleChannels, *BundleReleases)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleChannels)
	for i := range nodes {
		if nodes[i].CurrentReleaseId == nil {
			continue
		}
		fk := *nodes[i].CurrentReleaseId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundlereleases.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "currentReleaseId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BundleChannelsQuery) loadReleases(ctx context.Context, query *BundleReleasesQuery, nodes []*BundleChannels, init func(*BundleChannels), assign func(*BundleChannels, *BundleReleases)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleChannels)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundlereleases.FieldChannelId)
	}
	query.Where(predicate.BundleReleases(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlechannels.ReleasesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ChannelId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "channelId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleChannelsQuery) loadRollouts(ctx context.Context, query *BundleDeliveryRolloutsQuery, nodes []*BundleChannels, init func(*BundleChannels), assign func(*BundleChannels, *BundleDeliveryRollouts)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleChannels)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundledeliveryrollouts.FieldChannelId)
	}
	query.Where(predicate.BundleDeliveryRollouts(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlechannels.RolloutsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ChannelId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "channelId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *BundleChannelsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *BundleChannelsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(bundlechannels.Table, bundlechannels.Columns, sqlgraph.NewFieldSpec(bundlechannels.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bundlechannels.FieldID)
		for i := range fields {
			if fields[i] != bundlechannels.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withBundle != nil {
			_spec.Node.AddColumnOnce(bundlechannels.FieldBundleId)
		}
		if _q.withCurrentRelease != nil {
			_spec.Node.AddColumnOnce(bundlechannels.FieldCurrentReleaseId)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *BundleChannelsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(bundlechannels.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = bundlechannels.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *BundleChannelsQuery) ForUpdate(opts ...sql.LockOption) *BundleChannelsQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *BundleChannelsQuery) ForShare(opts ...sql.LockOption) *BundleChannelsQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *BundleChannelsQuery) Modify(modifiers ...func(s *sql.Selector)) *BundleChannelsSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// BundleChannelsGroupBy is the group-by builder for BundleChannels entities.
type BundleChannelsGroupBy struct {
	selector
	build *BundleChannelsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *BundleChannelsGroupBy) Aggregate(fns ...AggregateFunc) *BundleChannelsGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *BundleChannelsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BundleChannelsQuery, *BundleChannelsGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *BundleChannelsGroupBy) sqlScan(ctx context.Context, root *BundleChannelsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BundleChannelsSelect is the builder for selecting fields of BundleChannels entities.
type BundleChannelsSelect struct {
	*BundleChannelsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *BundleChannelsSelect) Aggregate(fns ...AggregateFunc) *BundleChannelsSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *BundleChannelsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BundleChannelsQuery, *BundleChannelsSelect](ctx, _s.BundleChannelsQuery, _s, _s.inters, v)
}

func (_s *BundleChannelsSelect) sqlScan(ctx context.Context, root *BundleChannelsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *BundleChannelsSelect) Modify(modifiers ...func(s *sql.Selector)) *BundleChannelsSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
