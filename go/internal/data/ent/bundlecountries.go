// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"trading/control-gateway/internal/data/ent/bundlecountries"
	"trading/control-gateway/internal/data/ent/bundles"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// BundleCountries is the model entity for the BundleCountries schema.
type BundleCountries struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// BundleId holds the value of the "bundleId" field.
	BundleId uuid.UUID `json:"bundleId,omitempty"`
	// CountryCode holds the value of the "countryCode" field.
	CountryCode string `json:"countryCode,omitempty"`
	// IsAvailable holds the value of the "isAvailable" field.
	IsAvailable bool `json:"isAvailable,omitempty"`
	// PriceOverride holds the value of the "priceOverride" field.
	PriceOverride *float64 `json:"priceOverride,omitempty"`
	// Currency holds the value of the "currency" field.
	Currency *string `json:"currency,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BundleCountriesQuery when eager-loading is set.
	Edges        BundleCountriesEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BundleCountriesEdges holds the relations/edges for other nodes in the graph.
type BundleCountriesEdges struct {
	// Bundle holds the value of the bundle edge.
	Bundle *Bundles `json:"bundle,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BundleOrErr returns the Bundle value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BundleCountriesEdges) BundleOrErr() (*Bundles, error) {
	if e.Bundle != nil {
		return e.Bundle, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: bundles.Label}
	}
	return nil, &NotLoadedError{edge: "bundle"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BundleCountries) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bundlecountries.FieldIsAvailable:
			values[i] = new(sql.NullBool)
		case bundlecountries.FieldPriceOverride:
			values[i] = new(sql.NullFloat64)
		case bundlecountries.FieldCountryCode, bundlecountries.FieldCurrency:
			values[i] = new(sql.NullString)
		case bundlecountries.FieldID, bundlecountries.FieldBundleId:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BundleCountries fields.
func (_m *BundleCountries) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bundlecountries.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case bundlecountries.FieldBundleId:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field bundleId", values[i])
			} else if value != nil {
				_m.BundleId = *value
			}
		case bundlecountries.FieldCountryCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field countryCode", values[i])
			} else if value.Valid {
				_m.CountryCode = value.String
			}
		case bundlecountries.FieldIsAvailable:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field isAvailable", values[i])
			} else if value.Valid {
				_m.IsAvailable = value.Bool
			}
		case bundlecountries.FieldPriceOverride:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field priceOverride", values[i])
			} else if value.Valid {
				_m.PriceOverride = new(float64)
				*_m.PriceOverride = value.Float64
			}
		case bundlecountries.FieldCurrency:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field currency", values[i])
			} else if value.Valid {
				_m.Currency = new(string)
				*_m.Currency = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BundleCountries.
// This includes values selected through modifiers, order, etc.
func (_m *BundleCountries) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBundle queries the "bundle" edge of the BundleCountries entity.
func (_m *BundleCountries) QueryBundle() *BundlesQuery {
	return NewBundleCountriesClient(_m.config).QueryBundle(_m)
}

// Update returns a builder for updating this BundleCountries.
// Note that you need to call BundleCountries.Unwrap() before calling this method if this BundleCountries
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BundleCountries) Update() *BundleCountriesUpdateOne {
	return NewBundleCountriesClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BundleCountries entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BundleCountries) Unwrap() *BundleCountries {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BundleCountries is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BundleCountries) String() string {
	var builder strings.Builder
	builder.WriteString("BundleCountries(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("bundleId=")
	builder.WriteString(fmt.Sprintf("%v", _m.BundleId))
	builder.WriteString(", ")
	builder.WriteString("countryCode=")
	builder.WriteString(_m.CountryCode)
	builder.WriteString(", ")
	builder.WriteString("isAvailable=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsAvailable))
	builder.WriteString(", ")
	if v := _m.PriceOverride; v != nil {
		builder.WriteString("priceOverride=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := _m.Currency; v != nil {
		builder.WriteString("currency=")
		builder.WriteString(*v)
	}
	builder.WriteByte(')')
	return builder.String()
}

// BundleCountriesSlice is a parsable slice of BundleCountries.
type BundleCountriesSlice []*BundleCountries
