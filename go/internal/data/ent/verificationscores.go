// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"
	"trading/control-gateway/internal/data/ent/verificationruns"
	"trading/control-gateway/internal/data/ent/verificationscores"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// VerificationScores is the model entity for the VerificationScores schema.
type VerificationScores struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// RunId holds the value of the "runId" field.
	RunId uuid.UUID `json:"runId,omitempty"`
	// Version holds the value of the "version" field.
	Version string `json:"version,omitempty"`
	// Dimensions holds the value of the "dimensions" field.
	Dimensions json.RawMessage `json:"dimensions,omitempty"`
	// Overall holds the value of the "overall" field.
	Overall float64 `json:"overall,omitempty"`
	// Confidence holds the value of the "confidence" field.
	Confidence float64 `json:"confidence,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VerificationScoresQuery when eager-loading is set.
	Edges        VerificationScoresEdges `json:"edges"`
	selectValues sql.SelectValues
}

// VerificationScoresEdges holds the relations/edges for other nodes in the graph.
type VerificationScoresEdges struct {
	// Run holds the value of the run edge.
	Run *VerificationRuns `json:"run,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// RunOrErr returns the Run value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VerificationScoresEdges) RunOrErr() (*VerificationRuns, error) {
	if e.Run != nil {
		return e.Run, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: verificationruns.Label}
	}
	return nil, &NotLoadedError{edge: "run"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VerificationScores) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case verificationscores.FieldDimensions:
			values[i] = new([]byte)
		case verificationscores.FieldOverall, verificationscores.FieldConfidence:
			values[i] = new(sql.NullFloat64)
		case verificationscores.FieldVersion:
			values[i] = new(sql.NullString)
		case verificationscores.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case verificationscores.FieldID, verificationscores.FieldRunId:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VerificationScores fields.
func (_m *VerificationScores) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case verificationscores.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case verificationscores.FieldRunId:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field runId", values[i])
			} else if value != nil {
				_m.RunId = *value
			}
		case verificationscores.FieldVersion:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				_m.Version = value.String
			}
		case verificationscores.FieldDimensions:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field dimensions", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Dimensions); err != nil {
					return fmt.Errorf("unmarshal field dimensions: %w", err)
				}
			}
		case verificationscores.FieldOverall:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field overall", values[i])
			} else if value.Valid {
				_m.Overall = value.Float64
			}
		case verificationscores.FieldConfidence:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field confidence", values[i])
			} else if value.Valid {
				_m.Confidence = value.Float64
			}
		case verificationscores.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the VerificationScores.
// This includes values selected through modifiers, order, etc.
func (_m *VerificationScores) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRun queries the "run" edge of the VerificationScores entity.
func (_m *VerificationScores) QueryRun() *VerificationRunsQuery {
	return NewVerificationScoresClient(_m.config).QueryRun(_m)
}

// Update returns a builder for updating this VerificationScores.
// Note that you need to call VerificationScores.Unwrap() before calling this method if this VerificationScores
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *VerificationScores) Update() *VerificationScoresUpdateOne {
	return NewVerificationScoresClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the VerificationScores entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *VerificationScores) Unwrap() *VerificationScores {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: VerificationScores is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *VerificationScores) String() string {
	var builder strings.Builder
	builder.WriteString("VerificationScores(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("runId=")
	builder.WriteString(fmt.Sprintf("%v", _m.RunId))
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(_m.Version)
	builder.WriteString(", ")
	builder.WriteString("dimensions=")
	builder.WriteString(fmt.Sprintf("%v", _m.Dimensions))
	builder.WriteString(", ")
	builder.WriteString("overall=")
	builder.WriteString(fmt.Sprintf("%v", _m.Overall))
	builder.WriteString(", ")
	builder.WriteString("confidence=")
	builder.WriteString(fmt.Sprintf("%v", _m.Confidence))
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// VerificationScoresSlice is a parsable slice of VerificationScores.
type VerificationScoresSlice []*VerificationScores
