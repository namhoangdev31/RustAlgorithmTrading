// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"trading/control-gateway/internal/data/ent/predicate"
	"trading/control-gateway/internal/data/ent/quantinstrument"
	"trading/control-gateway/internal/data/ent/quantwatchlist"
	"trading/control-gateway/internal/data/ent/quantwatchlistitem"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// QuantWatchlistItemQuery is the builder for querying QuantWatchlistItem entities.
type QuantWatchlistItemQuery struct {
	config
	ctx            *QueryContext
	order          []quantwatchlistitem.OrderOption
	inters         []Interceptor
	predicates     []predicate.QuantWatchlistItem
	withWatchlist  *QuantWatchlistQuery
	withInstrument *QuantInstrumentQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the QuantWatchlistItemQuery builder.
func (_q *QuantWatchlistItemQuery) Where(ps ...predicate.QuantWatchlistItem) *QuantWatchlistItemQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *QuantWatchlistItemQuery) Limit(limit int) *QuantWatchlistItemQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *QuantWatchlistItemQuery) Offset(offset int) *QuantWatchlistItemQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *QuantWatchlistItemQuery) Unique(unique bool) *QuantWatchlistItemQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *QuantWatchlistItemQuery) Order(o ...quantwatchlistitem.OrderOption) *QuantWatchlistItemQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryWatchlist chains the current query on the "watchlist" edge.
func (_q *QuantWatchlistItemQuery) QueryWatchlist() *QuantWatchlistQuery {
	query := (&QuantWatchlistClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(quantwatchlistitem.Table, quantwatchlistitem.FieldID, selector),
			sqlgraph.To(quantwatchlist.Table, quantwatchlist.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, quantwatchlistitem.WatchlistTable, quantwatchlistitem.WatchlistColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryInstrument chains the current query on the "instrument" edge.
func (_q *QuantWatchlistItemQuery) QueryInstrument() *QuantInstrumentQuery {
	query := (&QuantInstrumentClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(quantwatchlistitem.Table, quantwatchlistitem.FieldID, selector),
			sqlgraph.To(quantinstrument.Table, quantinstrument.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, quantwatchlistitem.InstrumentTable, quantwatchlistitem.InstrumentColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first QuantWatchlistItem entity from the query.
// Returns a *NotFoundError when no QuantWatchlistItem was found.
func (_q *QuantWatchlistItemQuery) First(ctx context.Context) (*QuantWatchlistItem, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{quantwatchlistitem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *QuantWatchlistItemQuery) FirstX(ctx context.Context) *QuantWatchlistItem {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first QuantWatchlistItem ID from the query.
// Returns a *NotFoundError when no QuantWatchlistItem ID was found.
func (_q *QuantWatchlistItemQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{quantwatchlistitem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *QuantWatchlistItemQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single QuantWatchlistItem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one QuantWatchlistItem entity is found.
// Returns a *NotFoundError when no QuantWatchlistItem entities are found.
func (_q *QuantWatchlistItemQuery) Only(ctx context.Context) (*QuantWatchlistItem, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{quantwatchlistitem.Label}
	default:
		return nil, &NotSingularError{quantwatchlistitem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *QuantWatchlistItemQuery) OnlyX(ctx context.Context) *QuantWatchlistItem {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only QuantWatchlistItem ID in the query.
// Returns a *NotSingularError when more than one QuantWatchlistItem ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *QuantWatchlistItemQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{quantwatchlistitem.Label}
	default:
		err = &NotSingularError{quantwatchlistitem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *QuantWatchlistItemQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of QuantWatchlistItems.
func (_q *QuantWatchlistItemQuery) All(ctx context.Context) ([]*QuantWatchlistItem, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*QuantWatchlistItem, *QuantWatchlistItemQuery]()
	return withInterceptors[[]*QuantWatchlistItem](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *QuantWatchlistItemQuery) AllX(ctx context.Context) []*QuantWatchlistItem {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of QuantWatchlistItem IDs.
func (_q *QuantWatchlistItemQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(quantwatchlistitem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *QuantWatchlistItemQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *QuantWatchlistItemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*QuantWatchlistItemQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *QuantWatchlistItemQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *QuantWatchlistItemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *QuantWatchlistItemQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the QuantWatchlistItemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *QuantWatchlistItemQuery) Clone() *QuantWatchlistItemQuery {
	if _q == nil {
		return nil
	}
	return &QuantWatchlistItemQuery{
		config:         _q.config,
		ctx:            _q.ctx.Clone(),
		order:          append([]quantwatchlistitem.OrderOption{}, _q.order...),
		inters:         append([]Interceptor{}, _q.inters...),
		predicates:     append([]predicate.QuantWatchlistItem{}, _q.predicates...),
		withWatchlist:  _q.withWatchlist.Clone(),
		withInstrument: _q.withInstrument.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithWatchlist tells the query-builder to eager-load the nodes that are connected to
// the "watchlist" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *QuantWatchlistItemQuery) WithWatchlist(opts ...func(*QuantWatchlistQuery)) *QuantWatchlistItemQuery {
	query := (&QuantWatchlistClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withWatchlist = query
	return _q
}

// WithInstrument tells the query-builder to eager-load the nodes that are connected to
// the "instrument" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *QuantWatchlistItemQuery) WithInstrument(opts ...func(*QuantInstrumentQuery)) *QuantWatchlistItemQuery {
	query := (&QuantInstrumentClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withInstrument = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		WatchlistId uuid.UUID `json:"watchlistId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.QuantWatchlistItem.Query().
//		GroupBy(quantwatchlistitem.FieldWatchlistId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *QuantWatchlistItemQuery) GroupBy(field string, fields ...string) *QuantWatchlistItemGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &QuantWatchlistItemGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = quantwatchlistitem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		WatchlistId uuid.UUID `json:"watchlistId,omitempty"`
//	}
//
//	client.QuantWatchlistItem.Query().
//		Select(quantwatchlistitem.FieldWatchlistId).
//		Scan(ctx, &v)
func (_q *QuantWatchlistItemQuery) Select(fields ...string) *QuantWatchlistItemSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &QuantWatchlistItemSelect{QuantWatchlistItemQuery: _q}
	sbuild.label = quantwatchlistitem.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a QuantWatchlistItemSelect configured with the given aggregations.
func (_q *QuantWatchlistItemQuery) Aggregate(fns ...AggregateFunc) *QuantWatchlistItemSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *QuantWatchlistItemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !quantwatchlistitem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *QuantWatchlistItemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*QuantWatchlistItem, error) {
	var (
		nodes       = []*QuantWatchlistItem{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withWatchlist != nil,
			_q.withInstrument != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*QuantWatchlistItem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &QuantWatchlistItem{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withWatchlist; query != nil {
		if err := _q.loadWatchlist(ctx, query, nodes, nil,
			func(n *QuantWatchlistItem, e *QuantWatchlist) { n.Edges.Watchlist = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withInstrument; query != nil {
		if err := _q.loadInstrument(ctx, query, nodes, nil,
			func(n *QuantWatchlistItem, e *QuantInstrument) { n.Edges.Instrument = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *QuantWatchlistItemQuery) loadWatchlist(ctx context.Context, query *QuantWatchlistQuery, nodes []*QuantWatchlistItem, init func(*QuantWatchlistItem), assign func(*QuantWatchlistItem, *QuantWatchlist)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*QuantWatchlistItem)
	for i := range nodes {
		fk := nodes[i].WatchlistId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(quantwatchlist.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "watchlistId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *QuantWatchlistItemQuery) loadInstrument(ctx context.Context, query *QuantInstrumentQuery, nodes []*QuantWatchlistItem, init func(*QuantWatchlistItem), assign func(*QuantWatchlistItem, *QuantInstrument)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*QuantWatchlistItem)
	for i := range nodes {
		fk := nodes[i].InstrumentId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(quantinstrument.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "instrumentId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *QuantWatchlistItemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *QuantWatchlistItemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(quantwatchlistitem.Table, quantwatchlistitem.Columns, sqlgraph.NewFieldSpec(quantwatchlistitem.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, quantwatchlistitem.FieldID)
		for i := range fields {
			if fields[i] != quantwatchlistitem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withWatchlist != nil {
			_spec.Node.AddColumnOnce(quantwatchlistitem.FieldWatchlistId)
		}
		if _q.withInstrument != nil {
			_spec.Node.AddColumnOnce(quantwatchlistitem.FieldInstrumentId)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *QuantWatchlistItemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(quantwatchlistitem.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = quantwatchlistitem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *QuantWatchlistItemQuery) ForUpdate(opts ...sql.LockOption) *QuantWatchlistItemQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *QuantWatchlistItemQuery) ForShare(opts ...sql.LockOption) *QuantWatchlistItemQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *QuantWatchlistItemQuery) Modify(modifiers ...func(s *sql.Selector)) *QuantWatchlistItemSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// QuantWatchlistItemGroupBy is the group-by builder for QuantWatchlistItem entities.
type QuantWatchlistItemGroupBy struct {
	selector
	build *QuantWatchlistItemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *QuantWatchlistItemGroupBy) Aggregate(fns ...AggregateFunc) *QuantWatchlistItemGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *QuantWatchlistItemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*QuantWatchlistItemQuery, *QuantWatchlistItemGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *QuantWatchlistItemGroupBy) sqlScan(ctx context.Context, root *QuantWatchlistItemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// QuantWatchlistItemSelect is the builder for selecting fields of QuantWatchlistItem entities.
type QuantWatchlistItemSelect struct {
	*QuantWatchlistItemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *QuantWatchlistItemSelect) Aggregate(fns ...AggregateFunc) *QuantWatchlistItemSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *QuantWatchlistItemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*QuantWatchlistItemQuery, *QuantWatchlistItemSelect](ctx, _s.QuantWatchlistItemQuery, _s, _s.inters, v)
}

func (_s *QuantWatchlistItemSelect) sqlScan(ctx context.Context, root *QuantWatchlistItemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *QuantWatchlistItemSelect) Modify(modifiers ...func(s *sql.Selector)) *QuantWatchlistItemSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
