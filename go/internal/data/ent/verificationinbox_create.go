// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"
	"trading/control-gateway/internal/data/ent/verificationinbox"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// VerificationInboxCreate is the builder for creating a VerificationInbox entity.
type VerificationInboxCreate struct {
	config
	mutation *VerificationInboxMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetConsumer sets the "consumer" field.
func (_c *VerificationInboxCreate) SetConsumer(v string) *VerificationInboxCreate {
	_c.mutation.SetConsumer(v)
	return _c
}

// SetMessageId sets the "messageId" field.
func (_c *VerificationInboxCreate) SetMessageId(v string) *VerificationInboxCreate {
	_c.mutation.SetMessageId(v)
	return _c
}

// SetOutcome sets the "outcome" field.
func (_c *VerificationInboxCreate) SetOutcome(v string) *VerificationInboxCreate {
	_c.mutation.SetOutcome(v)
	return _c
}

// SetNillableOutcome sets the "outcome" field if the given value is not nil.
func (_c *VerificationInboxCreate) SetNillableOutcome(v *string) *VerificationInboxCreate {
	if v != nil {
		_c.SetOutcome(*v)
	}
	return _c
}

// SetProcessedAt sets the "processedAt" field.
func (_c *VerificationInboxCreate) SetProcessedAt(v time.Time) *VerificationInboxCreate {
	_c.mutation.SetProcessedAt(v)
	return _c
}

// SetID sets the "id" field.
func (_c *VerificationInboxCreate) SetID(v uuid.UUID) *VerificationInboxCreate {
	_c.mutation.SetID(v)
	return _c
}

// Mutation returns the VerificationInboxMutation object of the builder.
func (_c *VerificationInboxCreate) Mutation() *VerificationInboxMutation {
	return _c.mutation
}

// Save creates the VerificationInbox in the database.
func (_c *VerificationInboxCreate) Save(ctx context.Context) (*VerificationInbox, error) {
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *VerificationInboxCreate) SaveX(ctx context.Context) *VerificationInbox {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *VerificationInboxCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *VerificationInboxCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *VerificationInboxCreate) check() error {
	if _, ok := _c.mutation.Consumer(); !ok {
		return &ValidationError{Name: "consumer", err: errors.New(`ent: missing required field "VerificationInbox.consumer"`)}
	}
	if _, ok := _c.mutation.MessageId(); !ok {
		return &ValidationError{Name: "messageId", err: errors.New(`ent: missing required field "VerificationInbox.messageId"`)}
	}
	if _, ok := _c.mutation.ProcessedAt(); !ok {
		return &ValidationError{Name: "processedAt", err: errors.New(`ent: missing required field "VerificationInbox.processedAt"`)}
	}
	return nil
}

func (_c *VerificationInboxCreate) sqlSave(ctx context.Context) (*VerificationInbox, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *VerificationInboxCreate) createSpec() (*VerificationInbox, *sqlgraph.CreateSpec) {
	var (
		_node = &VerificationInbox{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(verificationinbox.Table, sqlgraph.NewFieldSpec(verificationinbox.FieldID, field.TypeUUID))
	)
	_spec.OnConflict = _c.conflict
	if id, ok := _c.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := _c.mutation.Consumer(); ok {
		_spec.SetField(verificationinbox.FieldConsumer, field.TypeString, value)
		_node.Consumer = value
	}
	if value, ok := _c.mutation.MessageId(); ok {
		_spec.SetField(verificationinbox.FieldMessageId, field.TypeString, value)
		_node.MessageId = value
	}
	if value, ok := _c.mutation.Outcome(); ok {
		_spec.SetField(verificationinbox.FieldOutcome, field.TypeString, value)
		_node.Outcome = &value
	}
	if value, ok := _c.mutation.ProcessedAt(); ok {
		_spec.SetField(verificationinbox.FieldProcessedAt, field.TypeTime, value)
		_node.ProcessedAt = value
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.VerificationInbox.Create().
//		SetConsumer(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.VerificationInboxUpsert) {
//			SetConsumer(v+v).
//		}).
//		Exec(ctx)
func (_c *VerificationInboxCreate) OnConflict(opts ...sql.ConflictOption) *VerificationInboxUpsertOne {
	_c.conflict = opts
	return &VerificationInboxUpsertOne{
		create: _c,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.VerificationInbox.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (_c *VerificationInboxCreate) OnConflictColumns(columns ...string) *VerificationInboxUpsertOne {
	_c.conflict = append(_c.conflict, sql.ConflictColumns(columns...))
	return &VerificationInboxUpsertOne{
		create: _c,
	}
}

type (
	// VerificationInboxUpsertOne is the builder for "upsert"-ing
	//  one VerificationInbox node.
	VerificationInboxUpsertOne struct {
		create *VerificationInboxCreate
	}

	// VerificationInboxUpsert is the "OnConflict" setter.
	VerificationInboxUpsert struct {
		*sql.UpdateSet
	}
)

// SetConsumer sets the "consumer" field.
func (u *VerificationInboxUpsert) SetConsumer(v string) *VerificationInboxUpsert {
	u.Set(verificationinbox.FieldConsumer, v)
	return u
}

// UpdateConsumer sets the "consumer" field to the value that was provided on create.
func (u *VerificationInboxUpsert) UpdateConsumer() *VerificationInboxUpsert {
	u.SetExcluded(verificationinbox.FieldConsumer)
	return u
}

// SetMessageId sets the "messageId" field.
func (u *VerificationInboxUpsert) SetMessageId(v string) *VerificationInboxUpsert {
	u.Set(verificationinbox.FieldMessageId, v)
	return u
}

// UpdateMessageId sets the "messageId" field to the value that was provided on create.
func (u *VerificationInboxUpsert) UpdateMessageId() *VerificationInboxUpsert {
	u.SetExcluded(verificationinbox.FieldMessageId)
	return u
}

// SetOutcome sets the "outcome" field.
func (u *VerificationInboxUpsert) SetOutcome(v string) *VerificationInboxUpsert {
	u.Set(verificationinbox.FieldOutcome, v)
	return u
}

// UpdateOutcome sets the "outcome" field to the value that was provided on create.
func (u *VerificationInboxUpsert) UpdateOutcome() *VerificationInboxUpsert {
	u.SetExcluded(verificationinbox.FieldOutcome)
	return u
}

// ClearOutcome clears the value of the "outcome" field.
func (u *VerificationInboxUpsert) ClearOutcome() *VerificationInboxUpsert {
	u.SetNull(verificationinbox.FieldOutcome)
	return u
}

// SetProcessedAt sets the "processedAt" field.
func (u *VerificationInboxUpsert) SetProcessedAt(v time.Time) *VerificationInboxUpsert {
	u.Set(verificationinbox.FieldProcessedAt, v)
	return u
}

// UpdateProcessedAt sets the "processedAt" field to the value that was provided on create.
func (u *VerificationInboxUpsert) UpdateProcessedAt() *VerificationInboxUpsert {
	u.SetExcluded(verificationinbox.FieldProcessedAt)
	return u
}

// UpdateNewValues updates the mutable fields using the new values that were set on create except the ID field.
// Using this option is equivalent to using:
//
//	client.VerificationInbox.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(verificationinbox.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *VerificationInboxUpsertOne) UpdateNewValues() *VerificationInboxUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		if _, exists := u.create.mutation.ID(); exists {
			s.SetIgnore(verificationinbox.FieldID)
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.VerificationInbox.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *VerificationInboxUpsertOne) Ignore() *VerificationInboxUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *VerificationInboxUpsertOne) DoNothing() *VerificationInboxUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the VerificationInboxCreate.OnConflict
// documentation for more info.
func (u *VerificationInboxUpsertOne) Update(set func(*VerificationInboxUpsert)) *VerificationInboxUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&VerificationInboxUpsert{UpdateSet: update})
	}))
	return u
}

// SetConsumer sets the "consumer" field.
func (u *VerificationInboxUpsertOne) SetConsumer(v string) *VerificationInboxUpsertOne {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.SetConsumer(v)
	})
}

// UpdateConsumer sets the "consumer" field to the value that was provided on create.
func (u *VerificationInboxUpsertOne) UpdateConsumer() *VerificationInboxUpsertOne {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.UpdateConsumer()
	})
}

// SetMessageId sets the "messageId" field.
func (u *VerificationInboxUpsertOne) SetMessageId(v string) *VerificationInboxUpsertOne {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.SetMessageId(v)
	})
}

// UpdateMessageId sets the "messageId" field to the value that was provided on create.
func (u *VerificationInboxUpsertOne) UpdateMessageId() *VerificationInboxUpsertOne {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.UpdateMessageId()
	})
}

// SetOutcome sets the "outcome" field.
func (u *VerificationInboxUpsertOne) SetOutcome(v string) *VerificationInboxUpsertOne {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.SetOutcome(v)
	})
}

// UpdateOutcome sets the "outcome" field to the value that was provided on create.
func (u *VerificationInboxUpsertOne) UpdateOutcome() *VerificationInboxUpsertOne {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.UpdateOutcome()
	})
}

// ClearOutcome clears the value of the "outcome" field.
func (u *VerificationInboxUpsertOne) ClearOutcome() *VerificationInboxUpsertOne {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.ClearOutcome()
	})
}

// SetProcessedAt sets the "processedAt" field.
func (u *VerificationInboxUpsertOne) SetProcessedAt(v time.Time) *VerificationInboxUpsertOne {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.SetProcessedAt(v)
	})
}

// UpdateProcessedAt sets the "processedAt" field to the value that was provided on create.
func (u *VerificationInboxUpsertOne) UpdateProcessedAt() *VerificationInboxUpsertOne {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.UpdateProcessedAt()
	})
}

// Exec executes the query.
func (u *VerificationInboxUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for VerificationInboxCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *VerificationInboxUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *VerificationInboxUpsertOne) ID(ctx context.Context) (id uuid.UUID, err error) {
	if u.create.driver.Dialect() == dialect.MySQL {
		// In case of "ON CONFLICT", there is no way to get back non-numeric ID
		// fields from the database since MySQL does not support the RETURNING clause.
		return id, errors.New("ent: VerificationInboxUpsertOne.ID is not supported by MySQL driver. Use VerificationInboxUpsertOne.Exec instead")
	}
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *VerificationInboxUpsertOne) IDX(ctx context.Context) uuid.UUID {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// VerificationInboxCreateBulk is the builder for creating many VerificationInbox entities in bulk.
type VerificationInboxCreateBulk struct {
	config
	err      error
	builders []*VerificationInboxCreate
	conflict []sql.ConflictOption
}

// Save creates the VerificationInbox entities in the database.
func (_c *VerificationInboxCreateBulk) Save(ctx context.Context) ([]*VerificationInbox, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*VerificationInbox, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*VerificationInboxMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = _c.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *VerificationInboxCreateBulk) SaveX(ctx context.Context) []*VerificationInbox {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *VerificationInboxCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *VerificationInboxCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.VerificationInbox.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.VerificationInboxUpsert) {
//			SetConsumer(v+v).
//		}).
//		Exec(ctx)
func (_c *VerificationInboxCreateBulk) OnConflict(opts ...sql.ConflictOption) *VerificationInboxUpsertBulk {
	_c.conflict = opts
	return &VerificationInboxUpsertBulk{
		create: _c,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.VerificationInbox.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (_c *VerificationInboxCreateBulk) OnConflictColumns(columns ...string) *VerificationInboxUpsertBulk {
	_c.conflict = append(_c.conflict, sql.ConflictColumns(columns...))
	return &VerificationInboxUpsertBulk{
		create: _c,
	}
}

// VerificationInboxUpsertBulk is the builder for "upsert"-ing
// a bulk of VerificationInbox nodes.
type VerificationInboxUpsertBulk struct {
	create *VerificationInboxCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.VerificationInbox.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(verificationinbox.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *VerificationInboxUpsertBulk) UpdateNewValues() *VerificationInboxUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		for _, b := range u.create.builders {
			if _, exists := b.mutation.ID(); exists {
				s.SetIgnore(verificationinbox.FieldID)
			}
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.VerificationInbox.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *VerificationInboxUpsertBulk) Ignore() *VerificationInboxUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *VerificationInboxUpsertBulk) DoNothing() *VerificationInboxUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the VerificationInboxCreateBulk.OnConflict
// documentation for more info.
func (u *VerificationInboxUpsertBulk) Update(set func(*VerificationInboxUpsert)) *VerificationInboxUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&VerificationInboxUpsert{UpdateSet: update})
	}))
	return u
}

// SetConsumer sets the "consumer" field.
func (u *VerificationInboxUpsertBulk) SetConsumer(v string) *VerificationInboxUpsertBulk {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.SetConsumer(v)
	})
}

// UpdateConsumer sets the "consumer" field to the value that was provided on create.
func (u *VerificationInboxUpsertBulk) UpdateConsumer() *VerificationInboxUpsertBulk {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.UpdateConsumer()
	})
}

// SetMessageId sets the "messageId" field.
func (u *VerificationInboxUpsertBulk) SetMessageId(v string) *VerificationInboxUpsertBulk {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.SetMessageId(v)
	})
}

// UpdateMessageId sets the "messageId" field to the value that was provided on create.
func (u *VerificationInboxUpsertBulk) UpdateMessageId() *VerificationInboxUpsertBulk {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.UpdateMessageId()
	})
}

// SetOutcome sets the "outcome" field.
func (u *VerificationInboxUpsertBulk) SetOutcome(v string) *VerificationInboxUpsertBulk {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.SetOutcome(v)
	})
}

// UpdateOutcome sets the "outcome" field to the value that was provided on create.
func (u *VerificationInboxUpsertBulk) UpdateOutcome() *VerificationInboxUpsertBulk {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.UpdateOutcome()
	})
}

// ClearOutcome clears the value of the "outcome" field.
func (u *VerificationInboxUpsertBulk) ClearOutcome() *VerificationInboxUpsertBulk {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.ClearOutcome()
	})
}

// SetProcessedAt sets the "processedAt" field.
func (u *VerificationInboxUpsertBulk) SetProcessedAt(v time.Time) *VerificationInboxUpsertBulk {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.SetProcessedAt(v)
	})
}

// UpdateProcessedAt sets the "processedAt" field to the value that was provided on create.
func (u *VerificationInboxUpsertBulk) UpdateProcessedAt() *VerificationInboxUpsertBulk {
	return u.Update(func(s *VerificationInboxUpsert) {
		s.UpdateProcessedAt()
	})
}

// Exec executes the query.
func (u *VerificationInboxUpsertBulk) Exec(ctx context.Context) error {
	if u.create.err != nil {
		return u.create.err
	}
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("ent: OnConflict was set for builder %d. Set it on the VerificationInboxCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for VerificationInboxCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *VerificationInboxUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
