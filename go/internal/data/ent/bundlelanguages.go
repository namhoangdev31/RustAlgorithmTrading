// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"trading/control-gateway/internal/data/ent/bundlelanguages"
	"trading/control-gateway/internal/data/ent/bundles"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// BundleLanguages is the model entity for the BundleLanguages schema.
type BundleLanguages struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// BundleId holds the value of the "bundleId" field.
	BundleId uuid.UUID `json:"bundleId,omitempty"`
	// LanguageCode holds the value of the "languageCode" field.
	LanguageCode string `json:"languageCode,omitempty"`
	// LanguageName holds the value of the "languageName" field.
	LanguageName *string `json:"languageName,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BundleLanguagesQuery when eager-loading is set.
	Edges        BundleLanguagesEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BundleLanguagesEdges holds the relations/edges for other nodes in the graph.
type BundleLanguagesEdges struct {
	// Bundle holds the value of the bundle edge.
	Bundle *Bundles `json:"bundle,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BundleOrErr returns the Bundle value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BundleLanguagesEdges) BundleOrErr() (*Bundles, error) {
	if e.Bundle != nil {
		return e.Bundle, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: bundles.Label}
	}
	return nil, &NotLoadedError{edge: "bundle"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BundleLanguages) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bundlelanguages.FieldLanguageCode, bundlelanguages.FieldLanguageName:
			values[i] = new(sql.NullString)
		case bundlelanguages.FieldID, bundlelanguages.FieldBundleId:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BundleLanguages fields.
func (_m *BundleLanguages) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bundlelanguages.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case bundlelanguages.FieldBundleId:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field bundleId", values[i])
			} else if value != nil {
				_m.BundleId = *value
			}
		case bundlelanguages.FieldLanguageCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field languageCode", values[i])
			} else if value.Valid {
				_m.LanguageCode = value.String
			}
		case bundlelanguages.FieldLanguageName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field languageName", values[i])
			} else if value.Valid {
				_m.LanguageName = new(string)
				*_m.LanguageName = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BundleLanguages.
// This includes values selected through modifiers, order, etc.
func (_m *BundleLanguages) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBundle queries the "bundle" edge of the BundleLanguages entity.
func (_m *BundleLanguages) QueryBundle() *BundlesQuery {
	return NewBundleLanguagesClient(_m.config).QueryBundle(_m)
}

// Update returns a builder for updating this BundleLanguages.
// Note that you need to call BundleLanguages.Unwrap() before calling this method if this BundleLanguages
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BundleLanguages) Update() *BundleLanguagesUpdateOne {
	return NewBundleLanguagesClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BundleLanguages entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BundleLanguages) Unwrap() *BundleLanguages {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BundleLanguages is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BundleLanguages) String() string {
	var builder strings.Builder
	builder.WriteString("BundleLanguages(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("bundleId=")
	builder.WriteString(fmt.Sprintf("%v", _m.BundleId))
	builder.WriteString(", ")
	builder.WriteString("languageCode=")
	builder.WriteString(_m.LanguageCode)
	builder.WriteString(", ")
	if v := _m.LanguageName; v != nil {
		builder.WriteString("languageName=")
		builder.WriteString(*v)
	}
	builder.WriteByte(')')
	return builder.String()
}

// BundleLanguagesSlice is a parsable slice of BundleLanguages.
type BundleLanguagesSlice []*BundleLanguages
