// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"
	"trading/control-gateway/internal/data/ent/form"
	"trading/control-gateway/internal/data/ent/formsubmission"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// FormSubmission is the model entity for the FormSubmission schema.
type FormSubmission struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Data holds the value of the "data" field.
	Data json.RawMessage `json:"data,omitempty"`
	// IpAddress holds the value of the "ipAddress" field.
	IpAddress *string `json:"ipAddress,omitempty"`
	// UserAgent holds the value of the "userAgent" field.
	UserAgent *string `json:"userAgent,omitempty"`
	// FormId holds the value of the "formId" field.
	FormId string `json:"formId,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FormSubmissionQuery when eager-loading is set.
	Edges        FormSubmissionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// FormSubmissionEdges holds the relations/edges for other nodes in the graph.
type FormSubmissionEdges struct {
	// Form holds the value of the form edge.
	Form *Form `json:"form,omitempty"`
	// WebhookDeliveries holds the value of the webhookDeliveries edge.
	WebhookDeliveries []*FormWebhookDelivery `json:"webhookDeliveries,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// FormOrErr returns the Form value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e FormSubmissionEdges) FormOrErr() (*Form, error) {
	if e.Form != nil {
		return e.Form, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: form.Label}
	}
	return nil, &NotLoadedError{edge: "form"}
}

// WebhookDeliveriesOrErr returns the WebhookDeliveries value or an error if the edge
// was not loaded in eager-loading.
func (e FormSubmissionEdges) WebhookDeliveriesOrErr() ([]*FormWebhookDelivery, error) {
	if e.loadedTypes[1] {
		return e.WebhookDeliveries, nil
	}
	return nil, &NotLoadedError{edge: "webhookDeliveries"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*FormSubmission) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case formsubmission.FieldData:
			values[i] = new([]byte)
		case formsubmission.FieldID, formsubmission.FieldIpAddress, formsubmission.FieldUserAgent, formsubmission.FieldFormId:
			values[i] = new(sql.NullString)
		case formsubmission.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the FormSubmission fields.
func (_m *FormSubmission) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case formsubmission.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case formsubmission.FieldData:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field data", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Data); err != nil {
					return fmt.Errorf("unmarshal field data: %w", err)
				}
			}
		case formsubmission.FieldIpAddress:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ipAddress", values[i])
			} else if value.Valid {
				_m.IpAddress = new(string)
				*_m.IpAddress = value.String
			}
		case formsubmission.FieldUserAgent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field userAgent", values[i])
			} else if value.Valid {
				_m.UserAgent = new(string)
				*_m.UserAgent = value.String
			}
		case formsubmission.FieldFormId:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field formId", values[i])
			} else if value.Valid {
				_m.FormId = value.String
			}
		case formsubmission.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the FormSubmission.
// This includes values selected through modifiers, order, etc.
func (_m *FormSubmission) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryForm queries the "form" edge of the FormSubmission entity.
func (_m *FormSubmission) QueryForm() *FormQuery {
	return NewFormSubmissionClient(_m.config).QueryForm(_m)
}

// QueryWebhookDeliveries queries the "webhookDeliveries" edge of the FormSubmission entity.
func (_m *FormSubmission) QueryWebhookDeliveries() *FormWebhookDeliveryQuery {
	return NewFormSubmissionClient(_m.config).QueryWebhookDeliveries(_m)
}

// Update returns a builder for updating this FormSubmission.
// Note that you need to call FormSubmission.Unwrap() before calling this method if this FormSubmission
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *FormSubmission) Update() *FormSubmissionUpdateOne {
	return NewFormSubmissionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the FormSubmission entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *FormSubmission) Unwrap() *FormSubmission {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: FormSubmission is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *FormSubmission) String() string {
	var builder strings.Builder
	builder.WriteString("FormSubmission(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("data=")
	builder.WriteString(fmt.Sprintf("%v", _m.Data))
	builder.WriteString(", ")
	if v := _m.IpAddress; v != nil {
		builder.WriteString("ipAddress=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := _m.UserAgent; v != nil {
		builder.WriteString("userAgent=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("formId=")
	builder.WriteString(_m.FormId)
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// FormSubmissions is a parsable slice of FormSubmission.
type FormSubmissions []*FormSubmission
