// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"trading/control-gateway/internal/data/ent/bundleartifacts"
	"trading/control-gateway/internal/data/ent/bundlereleases"
	"trading/control-gateway/internal/data/ent/bundles"
	"trading/control-gateway/internal/data/ent/predicate"
	"trading/control-gateway/internal/data/ent/project"
	"trading/control-gateway/internal/data/ent/verificationevidence"
	"trading/control-gateway/internal/data/ent/verificationfindings"
	"trading/control-gateway/internal/data/ent/verificationpipelineversions"
	"trading/control-gateway/internal/data/ent/verificationpolicyevaluations"
	"trading/control-gateway/internal/data/ent/verificationpolicyversions"
	"trading/control-gateway/internal/data/ent/verificationreports"
	"trading/control-gateway/internal/data/ent/verificationruns"
	"trading/control-gateway/internal/data/ent/verificationscores"
	"trading/control-gateway/internal/data/ent/verificationtasks"
	"trading/control-gateway/internal/data/ent/verificationtelemetryevents"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// VerificationRunsQuery is the builder for querying VerificationRuns entities.
type VerificationRunsQuery struct {
	config
	ctx                    *QueryContext
	order                  []verificationruns.OrderOption
	inters                 []Interceptor
	predicates             []predicate.VerificationRuns
	withProject            *ProjectQuery
	withBundle             *BundlesQuery
	withRelease            *BundleReleasesQuery
	withArtifact           *BundleArtifactsQuery
	withPipelineVersion    *VerificationPipelineVersionsQuery
	withPolicyVersion      *VerificationPolicyVersionsQuery
	withEligibleForRelease *BundleReleasesQuery
	withTasks              *VerificationTasksQuery
	withFindings           *VerificationFindingsQuery
	withEvidence           *VerificationEvidenceQuery
	withScore              *VerificationScoresQuery
	withEvaluation         *VerificationPolicyEvaluationsQuery
	withReport             *VerificationReportsQuery
	withTelemetry          *VerificationTelemetryEventsQuery
	modifiers              []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VerificationRunsQuery builder.
func (_q *VerificationRunsQuery) Where(ps ...predicate.VerificationRuns) *VerificationRunsQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *VerificationRunsQuery) Limit(limit int) *VerificationRunsQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *VerificationRunsQuery) Offset(offset int) *VerificationRunsQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *VerificationRunsQuery) Unique(unique bool) *VerificationRunsQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *VerificationRunsQuery) Order(o ...verificationruns.OrderOption) *VerificationRunsQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryProject chains the current query on the "project" edge.
func (_q *VerificationRunsQuery) QueryProject() *ProjectQuery {
	query := (&ProjectClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(project.Table, project.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, verificationruns.ProjectTable, verificationruns.ProjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBundle chains the current query on the "bundle" edge.
func (_q *VerificationRunsQuery) QueryBundle() *BundlesQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(bundles.Table, bundles.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, verificationruns.BundleTable, verificationruns.BundleColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRelease chains the current query on the "release" edge.
func (_q *VerificationRunsQuery) QueryRelease() *BundleReleasesQuery {
	query := (&BundleReleasesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(bundlereleases.Table, bundlereleases.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, verificationruns.ReleaseTable, verificationruns.ReleaseColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryArtifact chains the current query on the "artifact" edge.
func (_q *VerificationRunsQuery) QueryArtifact() *BundleArtifactsQuery {
	query := (&BundleArtifactsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(bundleartifacts.Table, bundleartifacts.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, verificationruns.ArtifactTable, verificationruns.ArtifactColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPipelineVersion chains the current query on the "pipelineVersion" edge.
func (_q *VerificationRunsQuery) QueryPipelineVersion() *VerificationPipelineVersionsQuery {
	query := (&VerificationPipelineVersionsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(verificationpipelineversions.Table, verificationpipelineversions.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, verificationruns.PipelineVersionTable, verificationruns.PipelineVersionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPolicyVersion chains the current query on the "policyVersion" edge.
func (_q *VerificationRunsQuery) QueryPolicyVersion() *VerificationPolicyVersionsQuery {
	query := (&VerificationPolicyVersionsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(verificationpolicyversions.Table, verificationpolicyversions.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, verificationruns.PolicyVersionTable, verificationruns.PolicyVersionColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEligibleForRelease chains the current query on the "eligibleForRelease" edge.
func (_q *VerificationRunsQuery) QueryEligibleForRelease() *BundleReleasesQuery {
	query := (&BundleReleasesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(bundlereleases.Table, bundlereleases.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, verificationruns.EligibleForReleaseTable, verificationruns.EligibleForReleaseColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTasks chains the current query on the "tasks" edge.
func (_q *VerificationRunsQuery) QueryTasks() *VerificationTasksQuery {
	query := (&VerificationTasksClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(verificationtasks.Table, verificationtasks.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, verificationruns.TasksTable, verificationruns.TasksColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFindings chains the current query on the "findings" edge.
func (_q *VerificationRunsQuery) QueryFindings() *VerificationFindingsQuery {
	query := (&VerificationFindingsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(verificationfindings.Table, verificationfindings.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, verificationruns.FindingsTable, verificationruns.FindingsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEvidence chains the current query on the "evidence" edge.
func (_q *VerificationRunsQuery) QueryEvidence() *VerificationEvidenceQuery {
	query := (&VerificationEvidenceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(verificationevidence.Table, verificationevidence.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, verificationruns.EvidenceTable, verificationruns.EvidenceColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryScore chains the current query on the "score" edge.
func (_q *VerificationRunsQuery) QueryScore() *VerificationScoresQuery {
	query := (&VerificationScoresClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(verificationscores.Table, verificationscores.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, verificationruns.ScoreTable, verificationruns.ScoreColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEvaluation chains the current query on the "evaluation" edge.
func (_q *VerificationRunsQuery) QueryEvaluation() *VerificationPolicyEvaluationsQuery {
	query := (&VerificationPolicyEvaluationsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(verificationpolicyevaluations.Table, verificationpolicyevaluations.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, verificationruns.EvaluationTable, verificationruns.EvaluationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReport chains the current query on the "report" edge.
func (_q *VerificationRunsQuery) QueryReport() *VerificationReportsQuery {
	query := (&VerificationReportsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(verificationreports.Table, verificationreports.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, verificationruns.ReportTable, verificationruns.ReportColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTelemetry chains the current query on the "telemetry" edge.
func (_q *VerificationRunsQuery) QueryTelemetry() *VerificationTelemetryEventsQuery {
	query := (&VerificationTelemetryEventsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationruns.Table, verificationruns.FieldID, selector),
			sqlgraph.To(verificationtelemetryevents.Table, verificationtelemetryevents.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, verificationruns.TelemetryTable, verificationruns.TelemetryColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first VerificationRuns entity from the query.
// Returns a *NotFoundError when no VerificationRuns was found.
func (_q *VerificationRunsQuery) First(ctx context.Context) (*VerificationRuns, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{verificationruns.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *VerificationRunsQuery) FirstX(ctx context.Context) *VerificationRuns {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first VerificationRuns ID from the query.
// Returns a *NotFoundError when no VerificationRuns ID was found.
func (_q *VerificationRunsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{verificationruns.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *VerificationRunsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single VerificationRuns entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one VerificationRuns entity is found.
// Returns a *NotFoundError when no VerificationRuns entities are found.
func (_q *VerificationRunsQuery) Only(ctx context.Context) (*VerificationRuns, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{verificationruns.Label}
	default:
		return nil, &NotSingularError{verificationruns.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *VerificationRunsQuery) OnlyX(ctx context.Context) *VerificationRuns {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only VerificationRuns ID in the query.
// Returns a *NotSingularError when more than one VerificationRuns ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *VerificationRunsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{verificationruns.Label}
	default:
		err = &NotSingularError{verificationruns.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *VerificationRunsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of VerificationRunsSlice.
func (_q *VerificationRunsQuery) All(ctx context.Context) ([]*VerificationRuns, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*VerificationRuns, *VerificationRunsQuery]()
	return withInterceptors[[]*VerificationRuns](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *VerificationRunsQuery) AllX(ctx context.Context) []*VerificationRuns {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of VerificationRuns IDs.
func (_q *VerificationRunsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(verificationruns.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *VerificationRunsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *VerificationRunsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*VerificationRunsQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *VerificationRunsQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *VerificationRunsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *VerificationRunsQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VerificationRunsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *VerificationRunsQuery) Clone() *VerificationRunsQuery {
	if _q == nil {
		return nil
	}
	return &VerificationRunsQuery{
		config:                 _q.config,
		ctx:                    _q.ctx.Clone(),
		order:                  append([]verificationruns.OrderOption{}, _q.order...),
		inters:                 append([]Interceptor{}, _q.inters...),
		predicates:             append([]predicate.VerificationRuns{}, _q.predicates...),
		withProject:            _q.withProject.Clone(),
		withBundle:             _q.withBundle.Clone(),
		withRelease:            _q.withRelease.Clone(),
		withArtifact:           _q.withArtifact.Clone(),
		withPipelineVersion:    _q.withPipelineVersion.Clone(),
		withPolicyVersion:      _q.withPolicyVersion.Clone(),
		withEligibleForRelease: _q.withEligibleForRelease.Clone(),
		withTasks:              _q.withTasks.Clone(),
		withFindings:           _q.withFindings.Clone(),
		withEvidence:           _q.withEvidence.Clone(),
		withScore:              _q.withScore.Clone(),
		withEvaluation:         _q.withEvaluation.Clone(),
		withReport:             _q.withReport.Clone(),
		withTelemetry:          _q.withTelemetry.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithProject tells the query-builder to eager-load the nodes that are connected to
// the "project" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithProject(opts ...func(*ProjectQuery)) *VerificationRunsQuery {
	query := (&ProjectClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withProject = query
	return _q
}

// WithBundle tells the query-builder to eager-load the nodes that are connected to
// the "bundle" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithBundle(opts ...func(*BundlesQuery)) *VerificationRunsQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBundle = query
	return _q
}

// WithRelease tells the query-builder to eager-load the nodes that are connected to
// the "release" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithRelease(opts ...func(*BundleReleasesQuery)) *VerificationRunsQuery {
	query := (&BundleReleasesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRelease = query
	return _q
}

// WithArtifact tells the query-builder to eager-load the nodes that are connected to
// the "artifact" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithArtifact(opts ...func(*BundleArtifactsQuery)) *VerificationRunsQuery {
	query := (&BundleArtifactsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withArtifact = query
	return _q
}

// WithPipelineVersion tells the query-builder to eager-load the nodes that are connected to
// the "pipelineVersion" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithPipelineVersion(opts ...func(*VerificationPipelineVersionsQuery)) *VerificationRunsQuery {
	query := (&VerificationPipelineVersionsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPipelineVersion = query
	return _q
}

// WithPolicyVersion tells the query-builder to eager-load the nodes that are connected to
// the "policyVersion" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithPolicyVersion(opts ...func(*VerificationPolicyVersionsQuery)) *VerificationRunsQuery {
	query := (&VerificationPolicyVersionsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPolicyVersion = query
	return _q
}

// WithEligibleForRelease tells the query-builder to eager-load the nodes that are connected to
// the "eligibleForRelease" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithEligibleForRelease(opts ...func(*BundleReleasesQuery)) *VerificationRunsQuery {
	query := (&BundleReleasesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEligibleForRelease = query
	return _q
}

// WithTasks tells the query-builder to eager-load the nodes that are connected to
// the "tasks" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithTasks(opts ...func(*VerificationTasksQuery)) *VerificationRunsQuery {
	query := (&VerificationTasksClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTasks = query
	return _q
}

// WithFindings tells the query-builder to eager-load the nodes that are connected to
// the "findings" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithFindings(opts ...func(*VerificationFindingsQuery)) *VerificationRunsQuery {
	query := (&VerificationFindingsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFindings = query
	return _q
}

// WithEvidence tells the query-builder to eager-load the nodes that are connected to
// the "evidence" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithEvidence(opts ...func(*VerificationEvidenceQuery)) *VerificationRunsQuery {
	query := (&VerificationEvidenceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEvidence = query
	return _q
}

// WithScore tells the query-builder to eager-load the nodes that are connected to
// the "score" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithScore(opts ...func(*VerificationScoresQuery)) *VerificationRunsQuery {
	query := (&VerificationScoresClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withScore = query
	return _q
}

// WithEvaluation tells the query-builder to eager-load the nodes that are connected to
// the "evaluation" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithEvaluation(opts ...func(*VerificationPolicyEvaluationsQuery)) *VerificationRunsQuery {
	query := (&VerificationPolicyEvaluationsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEvaluation = query
	return _q
}

// WithReport tells the query-builder to eager-load the nodes that are connected to
// the "report" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithReport(opts ...func(*VerificationReportsQuery)) *VerificationRunsQuery {
	query := (&VerificationReportsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withReport = query
	return _q
}

// WithTelemetry tells the query-builder to eager-load the nodes that are connected to
// the "telemetry" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationRunsQuery) WithTelemetry(opts ...func(*VerificationTelemetryEventsQuery)) *VerificationRunsQuery {
	query := (&VerificationTelemetryEventsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTelemetry = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ProjectId uuid.UUID `json:"projectId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.VerificationRuns.Query().
//		GroupBy(verificationruns.FieldProjectId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *VerificationRunsQuery) GroupBy(field string, fields ...string) *VerificationRunsGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &VerificationRunsGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = verificationruns.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ProjectId uuid.UUID `json:"projectId,omitempty"`
//	}
//
//	client.VerificationRuns.Query().
//		Select(verificationruns.FieldProjectId).
//		Scan(ctx, &v)
func (_q *VerificationRunsQuery) Select(fields ...string) *VerificationRunsSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &VerificationRunsSelect{VerificationRunsQuery: _q}
	sbuild.label = verificationruns.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a VerificationRunsSelect configured with the given aggregations.
func (_q *VerificationRunsQuery) Aggregate(fns ...AggregateFunc) *VerificationRunsSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *VerificationRunsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !verificationruns.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *VerificationRunsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*VerificationRuns, error) {
	var (
		nodes       = []*VerificationRuns{}
		_spec       = _q.querySpec()
		loadedTypes = [14]bool{
			_q.withProject != nil,
			_q.withBundle != nil,
			_q.withRelease != nil,
			_q.withArtifact != nil,
			_q.withPipelineVersion != nil,
			_q.withPolicyVersion != nil,
			_q.withEligibleForRelease != nil,
			_q.withTasks != nil,
			_q.withFindings != nil,
			_q.withEvidence != nil,
			_q.withScore != nil,
			_q.withEvaluation != nil,
			_q.withReport != nil,
			_q.withTelemetry != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*VerificationRuns).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &VerificationRuns{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withProject; query != nil {
		if err := _q.loadProject(ctx, query, nodes, nil,
			func(n *VerificationRuns, e *Project) { n.Edges.Project = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withBundle; query != nil {
		if err := _q.loadBundle(ctx, query, nodes, nil,
			func(n *VerificationRuns, e *Bundles) { n.Edges.Bundle = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRelease; query != nil {
		if err := _q.loadRelease(ctx, query, nodes, nil,
			func(n *VerificationRuns, e *BundleReleases) { n.Edges.Release = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withArtifact; query != nil {
		if err := _q.loadArtifact(ctx, query, nodes, nil,
			func(n *VerificationRuns, e *BundleArtifacts) { n.Edges.Artifact = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPipelineVersion; query != nil {
		if err := _q.loadPipelineVersion(ctx, query, nodes, nil,
			func(n *VerificationRuns, e *VerificationPipelineVersions) { n.Edges.PipelineVersion = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPolicyVersion; query != nil {
		if err := _q.loadPolicyVersion(ctx, query, nodes, nil,
			func(n *VerificationRuns, e *VerificationPolicyVersions) { n.Edges.PolicyVersion = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEligibleForRelease; query != nil {
		if err := _q.loadEligibleForRelease(ctx, query, nodes, nil,
			func(n *VerificationRuns, e *BundleReleases) { n.Edges.EligibleForRelease = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTasks; query != nil {
		if err := _q.loadTasks(ctx, query, nodes,
			func(n *VerificationRuns) { n.Edges.Tasks = []*VerificationTasks{} },
			func(n *VerificationRuns, e *VerificationTasks) { n.Edges.Tasks = append(n.Edges.Tasks, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withFindings; query != nil {
		if err := _q.loadFindings(ctx, query, nodes,
			func(n *VerificationRuns) { n.Edges.Findings = []*VerificationFindings{} },
			func(n *VerificationRuns, e *VerificationFindings) { n.Edges.Findings = append(n.Edges.Findings, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEvidence; query != nil {
		if err := _q.loadEvidence(ctx, query, nodes,
			func(n *VerificationRuns) { n.Edges.Evidence = []*VerificationEvidence{} },
			func(n *VerificationRuns, e *VerificationEvidence) { n.Edges.Evidence = append(n.Edges.Evidence, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withScore; query != nil {
		if err := _q.loadScore(ctx, query, nodes, nil,
			func(n *VerificationRuns, e *VerificationScores) { n.Edges.Score = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEvaluation; query != nil {
		if err := _q.loadEvaluation(ctx, query, nodes, nil,
			func(n *VerificationRuns, e *VerificationPolicyEvaluations) { n.Edges.Evaluation = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withReport; query != nil {
		if err := _q.loadReport(ctx, query, nodes, nil,
			func(n *VerificationRuns, e *VerificationReports) { n.Edges.Report = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTelemetry; query != nil {
		if err := _q.loadTelemetry(ctx, query, nodes,
			func(n *VerificationRuns) { n.Edges.Telemetry = []*VerificationTelemetryEvents{} },
			func(n *VerificationRuns, e *VerificationTelemetryEvents) {
				n.Edges.Telemetry = append(n.Edges.Telemetry, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *VerificationRunsQuery) loadProject(ctx context.Context, query *ProjectQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *Project)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*VerificationRuns)
	for i := range nodes {
		fk := nodes[i].ProjectId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(project.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "projectId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *VerificationRunsQuery) loadBundle(ctx context.Context, query *BundlesQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *Bundles)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*VerificationRuns)
	for i := range nodes {
		fk := nodes[i].BundleId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundles.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "bundleId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *VerificationRunsQuery) loadRelease(ctx context.Context, query *BundleReleasesQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *BundleReleases)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*VerificationRuns)
	for i := range nodes {
		fk := nodes[i].ReleaseId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundlereleases.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "releaseId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *VerificationRunsQuery) loadArtifact(ctx context.Context, query *BundleArtifactsQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *BundleArtifacts)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*VerificationRuns)
	for i := range nodes {
		fk := nodes[i].ArtifactId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundleartifacts.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "artifactId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *VerificationRunsQuery) loadPipelineVersion(ctx context.Context, query *VerificationPipelineVersionsQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *VerificationPipelineVersions)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*VerificationRuns)
	for i := range nodes {
		fk := nodes[i].PipelineVersionId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(verificationpipelineversions.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "pipelineVersionId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *VerificationRunsQuery) loadPolicyVersion(ctx context.Context, query *VerificationPolicyVersionsQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *VerificationPolicyVersions)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*VerificationRuns)
	for i := range nodes {
		fk := nodes[i].PolicyVersionId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(verificationpolicyversions.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "policyVersionId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *VerificationRunsQuery) loadEligibleForRelease(ctx context.Context, query *BundleReleasesQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *BundleReleases)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationRuns)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundlereleases.FieldEligibleVerificationRunId)
	}
	query.Where(predicate.BundleReleases(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationruns.EligibleForReleaseColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.EligibleVerificationRunId
		if fk == nil {
			return fmt.Errorf(`foreign-key "eligibleVerificationRunId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "eligibleVerificationRunId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *VerificationRunsQuery) loadTasks(ctx context.Context, query *VerificationTasksQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *VerificationTasks)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationRuns)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(verificationtasks.FieldRunId)
	}
	query.Where(predicate.VerificationTasks(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationruns.TasksColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RunId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "runId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *VerificationRunsQuery) loadFindings(ctx context.Context, query *VerificationFindingsQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *VerificationFindings)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationRuns)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(verificationfindings.FieldRunId)
	}
	query.Where(predicate.VerificationFindings(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationruns.FindingsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RunId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "runId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *VerificationRunsQuery) loadEvidence(ctx context.Context, query *VerificationEvidenceQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *VerificationEvidence)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationRuns)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(verificationevidence.FieldRunId)
	}
	query.Where(predicate.VerificationEvidence(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationruns.EvidenceColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RunId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "runId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *VerificationRunsQuery) loadScore(ctx context.Context, query *VerificationScoresQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *VerificationScores)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationRuns)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(verificationscores.FieldRunId)
	}
	query.Where(predicate.VerificationScores(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationruns.ScoreColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RunId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "runId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *VerificationRunsQuery) loadEvaluation(ctx context.Context, query *VerificationPolicyEvaluationsQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *VerificationPolicyEvaluations)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationRuns)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(verificationpolicyevaluations.FieldRunId)
	}
	query.Where(predicate.VerificationPolicyEvaluations(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationruns.EvaluationColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RunId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "runId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *VerificationRunsQuery) loadReport(ctx context.Context, query *VerificationReportsQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *VerificationReports)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationRuns)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(verificationreports.FieldRunId)
	}
	query.Where(predicate.VerificationReports(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationruns.ReportColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RunId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "runId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *VerificationRunsQuery) loadTelemetry(ctx context.Context, query *VerificationTelemetryEventsQuery, nodes []*VerificationRuns, init func(*VerificationRuns), assign func(*VerificationRuns, *VerificationTelemetryEvents)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationRuns)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(verificationtelemetryevents.FieldRunId)
	}
	query.Where(predicate.VerificationTelemetryEvents(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationruns.TelemetryColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RunId
		if fk == nil {
			return fmt.Errorf(`foreign-key "runId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "runId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *VerificationRunsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *VerificationRunsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(verificationruns.Table, verificationruns.Columns, sqlgraph.NewFieldSpec(verificationruns.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, verificationruns.FieldID)
		for i := range fields {
			if fields[i] != verificationruns.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withProject != nil {
			_spec.Node.AddColumnOnce(verificationruns.FieldProjectId)
		}
		if _q.withBundle != nil {
			_spec.Node.AddColumnOnce(verificationruns.FieldBundleId)
		}
		if _q.withRelease != nil {
			_spec.Node.AddColumnOnce(verificationruns.FieldReleaseId)
		}
		if _q.withArtifact != nil {
			_spec.Node.AddColumnOnce(verificationruns.FieldArtifactId)
		}
		if _q.withPipelineVersion != nil {
			_spec.Node.AddColumnOnce(verificationruns.FieldPipelineVersionId)
		}
		if _q.withPolicyVersion != nil {
			_spec.Node.AddColumnOnce(verificationruns.FieldPolicyVersionId)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *VerificationRunsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(verificationruns.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = verificationruns.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *VerificationRunsQuery) ForUpdate(opts ...sql.LockOption) *VerificationRunsQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *VerificationRunsQuery) ForShare(opts ...sql.LockOption) *VerificationRunsQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *VerificationRunsQuery) Modify(modifiers ...func(s *sql.Selector)) *VerificationRunsSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// VerificationRunsGroupBy is the group-by builder for VerificationRuns entities.
type VerificationRunsGroupBy struct {
	selector
	build *VerificationRunsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *VerificationRunsGroupBy) Aggregate(fns ...AggregateFunc) *VerificationRunsGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *VerificationRunsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VerificationRunsQuery, *VerificationRunsGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *VerificationRunsGroupBy) sqlScan(ctx context.Context, root *VerificationRunsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// VerificationRunsSelect is the builder for selecting fields of VerificationRuns entities.
type VerificationRunsSelect struct {
	*VerificationRunsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *VerificationRunsSelect) Aggregate(fns ...AggregateFunc) *VerificationRunsSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *VerificationRunsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VerificationRunsQuery, *VerificationRunsSelect](ctx, _s.VerificationRunsQuery, _s, _s.inters, v)
}

func (_s *VerificationRunsSelect) sqlScan(ctx context.Context, root *VerificationRunsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *VerificationRunsSelect) Modify(modifiers ...func(s *sql.Selector)) *VerificationRunsSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
