// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"trading/control-gateway/internal/data/ent/quantinstrument"
	"trading/control-gateway/internal/data/ent/quantwatchlist"
	"trading/control-gateway/internal/data/ent/quantwatchlistitem"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// QuantWatchlistItem is the model entity for the QuantWatchlistItem schema.
type QuantWatchlistItem struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// WatchlistId holds the value of the "watchlistId" field.
	WatchlistId uuid.UUID `json:"watchlistId,omitempty"`
	// InstrumentId holds the value of the "instrumentId" field.
	InstrumentId uuid.UUID `json:"instrumentId,omitempty"`
	// SortOrder holds the value of the "sortOrder" field.
	SortOrder int `json:"sortOrder,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the QuantWatchlistItemQuery when eager-loading is set.
	Edges        QuantWatchlistItemEdges `json:"edges"`
	selectValues sql.SelectValues
}

// QuantWatchlistItemEdges holds the relations/edges for other nodes in the graph.
type QuantWatchlistItemEdges struct {
	// Watchlist holds the value of the watchlist edge.
	Watchlist *QuantWatchlist `json:"watchlist,omitempty"`
	// Instrument holds the value of the instrument edge.
	Instrument *QuantInstrument `json:"instrument,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// WatchlistOrErr returns the Watchlist value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e QuantWatchlistItemEdges) WatchlistOrErr() (*QuantWatchlist, error) {
	if e.Watchlist != nil {
		return e.Watchlist, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: quantwatchlist.Label}
	}
	return nil, &NotLoadedError{edge: "watchlist"}
}

// InstrumentOrErr returns the Instrument value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e QuantWatchlistItemEdges) InstrumentOrErr() (*QuantInstrument, error) {
	if e.Instrument != nil {
		return e.Instrument, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: quantinstrument.Label}
	}
	return nil, &NotLoadedError{edge: "instrument"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*QuantWatchlistItem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case quantwatchlistitem.FieldSortOrder:
			values[i] = new(sql.NullInt64)
		case quantwatchlistitem.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case quantwatchlistitem.FieldID, quantwatchlistitem.FieldWatchlistId, quantwatchlistitem.FieldInstrumentId:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the QuantWatchlistItem fields.
func (_m *QuantWatchlistItem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case quantwatchlistitem.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case quantwatchlistitem.FieldWatchlistId:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field watchlistId", values[i])
			} else if value != nil {
				_m.WatchlistId = *value
			}
		case quantwatchlistitem.FieldInstrumentId:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field instrumentId", values[i])
			} else if value != nil {
				_m.InstrumentId = *value
			}
		case quantwatchlistitem.FieldSortOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sortOrder", values[i])
			} else if value.Valid {
				_m.SortOrder = int(value.Int64)
			}
		case quantwatchlistitem.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the QuantWatchlistItem.
// This includes values selected through modifiers, order, etc.
func (_m *QuantWatchlistItem) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryWatchlist queries the "watchlist" edge of the QuantWatchlistItem entity.
func (_m *QuantWatchlistItem) QueryWatchlist() *QuantWatchlistQuery {
	return NewQuantWatchlistItemClient(_m.config).QueryWatchlist(_m)
}

// QueryInstrument queries the "instrument" edge of the QuantWatchlistItem entity.
func (_m *QuantWatchlistItem) QueryInstrument() *QuantInstrumentQuery {
	return NewQuantWatchlistItemClient(_m.config).QueryInstrument(_m)
}

// Update returns a builder for updating this QuantWatchlistItem.
// Note that you need to call QuantWatchlistItem.Unwrap() before calling this method if this QuantWatchlistItem
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *QuantWatchlistItem) Update() *QuantWatchlistItemUpdateOne {
	return NewQuantWatchlistItemClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the QuantWatchlistItem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *QuantWatchlistItem) Unwrap() *QuantWatchlistItem {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: QuantWatchlistItem is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *QuantWatchlistItem) String() string {
	var builder strings.Builder
	builder.WriteString("QuantWatchlistItem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("watchlistId=")
	builder.WriteString(fmt.Sprintf("%v", _m.WatchlistId))
	builder.WriteString(", ")
	builder.WriteString("instrumentId=")
	builder.WriteString(fmt.Sprintf("%v", _m.InstrumentId))
	builder.WriteString(", ")
	builder.WriteString("sortOrder=")
	builder.WriteString(fmt.Sprintf("%v", _m.SortOrder))
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// QuantWatchlistItems is a parsable slice of QuantWatchlistItem.
type QuantWatchlistItems []*QuantWatchlistItem
