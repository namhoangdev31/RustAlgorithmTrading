// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"trading/control-gateway/internal/data/ent/bundlereleaseoverrideapprovals"
	"trading/control-gateway/internal/data/ent/bundlereleaseoverridesv2"
	"trading/control-gateway/internal/data/ent/bundlereleases"
	"trading/control-gateway/internal/data/ent/bundles"
	"trading/control-gateway/internal/data/ent/predicate"
	"trading/control-gateway/internal/data/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// BundleReleaseOverridesV2Query is the builder for querying BundleReleaseOverridesV2 entities.
type BundleReleaseOverridesV2Query struct {
	config
	ctx           *QueryContext
	order         []bundlereleaseoverridesv2.OrderOption
	inters        []Interceptor
	predicates    []predicate.BundleReleaseOverridesV2
	withBundle    *BundlesQuery
	withRelease   *BundleReleasesQuery
	withCreatedBy *UserQuery
	withApprovals *BundleReleaseOverrideApprovalsQuery
	modifiers     []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BundleReleaseOverridesV2Query builder.
func (_q *BundleReleaseOverridesV2Query) Where(ps ...predicate.BundleReleaseOverridesV2) *BundleReleaseOverridesV2Query {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *BundleReleaseOverridesV2Query) Limit(limit int) *BundleReleaseOverridesV2Query {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *BundleReleaseOverridesV2Query) Offset(offset int) *BundleReleaseOverridesV2Query {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *BundleReleaseOverridesV2Query) Unique(unique bool) *BundleReleaseOverridesV2Query {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *BundleReleaseOverridesV2Query) Order(o ...bundlereleaseoverridesv2.OrderOption) *BundleReleaseOverridesV2Query {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryBundle chains the current query on the "bundle" edge.
func (_q *BundleReleaseOverridesV2Query) QueryBundle() *BundlesQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleaseoverridesv2.Table, bundlereleaseoverridesv2.FieldID, selector),
			sqlgraph.To(bundles.Table, bundles.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlereleaseoverridesv2.BundleTable, bundlereleaseoverridesv2.BundleColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRelease chains the current query on the "release" edge.
func (_q *BundleReleaseOverridesV2Query) QueryRelease() *BundleReleasesQuery {
	query := (&BundleReleasesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleaseoverridesv2.Table, bundlereleaseoverridesv2.FieldID, selector),
			sqlgraph.To(bundlereleases.Table, bundlereleases.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlereleaseoverridesv2.ReleaseTable, bundlereleaseoverridesv2.ReleaseColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCreatedBy chains the current query on the "createdBy" edge.
func (_q *BundleReleaseOverridesV2Query) QueryCreatedBy() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleaseoverridesv2.Table, bundlereleaseoverridesv2.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlereleaseoverridesv2.CreatedByTable, bundlereleaseoverridesv2.CreatedByColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryApprovals chains the current query on the "approvals" edge.
func (_q *BundleReleaseOverridesV2Query) QueryApprovals() *BundleReleaseOverrideApprovalsQuery {
	query := (&BundleReleaseOverrideApprovalsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleaseoverridesv2.Table, bundlereleaseoverridesv2.FieldID, selector),
			sqlgraph.To(bundlereleaseoverrideapprovals.Table, bundlereleaseoverrideapprovals.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleaseoverridesv2.ApprovalsTable, bundlereleaseoverridesv2.ApprovalsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BundleReleaseOverridesV2 entity from the query.
// Returns a *NotFoundError when no BundleReleaseOverridesV2 was found.
func (_q *BundleReleaseOverridesV2Query) First(ctx context.Context) (*BundleReleaseOverridesV2, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bundlereleaseoverridesv2.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *BundleReleaseOverridesV2Query) FirstX(ctx context.Context) *BundleReleaseOverridesV2 {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BundleReleaseOverridesV2 ID from the query.
// Returns a *NotFoundError when no BundleReleaseOverridesV2 ID was found.
func (_q *BundleReleaseOverridesV2Query) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bundlereleaseoverridesv2.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *BundleReleaseOverridesV2Query) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BundleReleaseOverridesV2 entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BundleReleaseOverridesV2 entity is found.
// Returns a *NotFoundError when no BundleReleaseOverridesV2 entities are found.
func (_q *BundleReleaseOverridesV2Query) Only(ctx context.Context) (*BundleReleaseOverridesV2, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bundlereleaseoverridesv2.Label}
	default:
		return nil, &NotSingularError{bundlereleaseoverridesv2.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *BundleReleaseOverridesV2Query) OnlyX(ctx context.Context) *BundleReleaseOverridesV2 {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BundleReleaseOverridesV2 ID in the query.
// Returns a *NotSingularError when more than one BundleReleaseOverridesV2 ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *BundleReleaseOverridesV2Query) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bundlereleaseoverridesv2.Label}
	default:
		err = &NotSingularError{bundlereleaseoverridesv2.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *BundleReleaseOverridesV2Query) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BundleReleaseOverridesV2s.
func (_q *BundleReleaseOverridesV2Query) All(ctx context.Context) ([]*BundleReleaseOverridesV2, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BundleReleaseOverridesV2, *BundleReleaseOverridesV2Query]()
	return withInterceptors[[]*BundleReleaseOverridesV2](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *BundleReleaseOverridesV2Query) AllX(ctx context.Context) []*BundleReleaseOverridesV2 {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BundleReleaseOverridesV2 IDs.
func (_q *BundleReleaseOverridesV2Query) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(bundlereleaseoverridesv2.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *BundleReleaseOverridesV2Query) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *BundleReleaseOverridesV2Query) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*BundleReleaseOverridesV2Query](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *BundleReleaseOverridesV2Query) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *BundleReleaseOverridesV2Query) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *BundleReleaseOverridesV2Query) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BundleReleaseOverridesV2Query builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *BundleReleaseOverridesV2Query) Clone() *BundleReleaseOverridesV2Query {
	if _q == nil {
		return nil
	}
	return &BundleReleaseOverridesV2Query{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]bundlereleaseoverridesv2.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.BundleReleaseOverridesV2{}, _q.predicates...),
		withBundle:    _q.withBundle.Clone(),
		withRelease:   _q.withRelease.Clone(),
		withCreatedBy: _q.withCreatedBy.Clone(),
		withApprovals: _q.withApprovals.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithBundle tells the query-builder to eager-load the nodes that are connected to
// the "bundle" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleaseOverridesV2Query) WithBundle(opts ...func(*BundlesQuery)) *BundleReleaseOverridesV2Query {
	query := (&BundlesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBundle = query
	return _q
}

// WithRelease tells the query-builder to eager-load the nodes that are connected to
// the "release" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleaseOverridesV2Query) WithRelease(opts ...func(*BundleReleasesQuery)) *BundleReleaseOverridesV2Query {
	query := (&BundleReleasesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRelease = query
	return _q
}

// WithCreatedBy tells the query-builder to eager-load the nodes that are connected to
// the "createdBy" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleaseOverridesV2Query) WithCreatedBy(opts ...func(*UserQuery)) *BundleReleaseOverridesV2Query {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCreatedBy = query
	return _q
}

// WithApprovals tells the query-builder to eager-load the nodes that are connected to
// the "approvals" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleaseOverridesV2Query) WithApprovals(opts ...func(*BundleReleaseOverrideApprovalsQuery)) *BundleReleaseOverridesV2Query {
	query := (&BundleReleaseOverrideApprovalsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withApprovals = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BundleId uuid.UUID `json:"bundleId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BundleReleaseOverridesV2.Query().
//		GroupBy(bundlereleaseoverridesv2.FieldBundleId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *BundleReleaseOverridesV2Query) GroupBy(field string, fields ...string) *BundleReleaseOverridesV2GroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BundleReleaseOverridesV2GroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = bundlereleaseoverridesv2.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BundleId uuid.UUID `json:"bundleId,omitempty"`
//	}
//
//	client.BundleReleaseOverridesV2.Query().
//		Select(bundlereleaseoverridesv2.FieldBundleId).
//		Scan(ctx, &v)
func (_q *BundleReleaseOverridesV2Query) Select(fields ...string) *BundleReleaseOverridesV2Select {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &BundleReleaseOverridesV2Select{BundleReleaseOverridesV2Query: _q}
	sbuild.label = bundlereleaseoverridesv2.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BundleReleaseOverridesV2Select configured with the given aggregations.
func (_q *BundleReleaseOverridesV2Query) Aggregate(fns ...AggregateFunc) *BundleReleaseOverridesV2Select {
	return _q.Select().Aggregate(fns...)
}

func (_q *BundleReleaseOverridesV2Query) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !bundlereleaseoverridesv2.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *BundleReleaseOverridesV2Query) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BundleReleaseOverridesV2, error) {
	var (
		nodes       = []*BundleReleaseOverridesV2{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withBundle != nil,
			_q.withRelease != nil,
			_q.withCreatedBy != nil,
			_q.withApprovals != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BundleReleaseOverridesV2).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BundleReleaseOverridesV2{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withBundle; query != nil {
		if err := _q.loadBundle(ctx, query, nodes, nil,
			func(n *BundleReleaseOverridesV2, e *Bundles) { n.Edges.Bundle = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRelease; query != nil {
		if err := _q.loadRelease(ctx, query, nodes, nil,
			func(n *BundleReleaseOverridesV2, e *BundleReleases) { n.Edges.Release = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCreatedBy; query != nil {
		if err := _q.loadCreatedBy(ctx, query, nodes, nil,
			func(n *BundleReleaseOverridesV2, e *User) { n.Edges.CreatedBy = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withApprovals; query != nil {
		if err := _q.loadApprovals(ctx, query, nodes,
			func(n *BundleReleaseOverridesV2) { n.Edges.Approvals = []*BundleReleaseOverrideApprovals{} },
			func(n *BundleReleaseOverridesV2, e *BundleReleaseOverrideApprovals) {
				n.Edges.Approvals = append(n.Edges.Approvals, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *BundleReleaseOverridesV2Query) loadBundle(ctx context.Context, query *BundlesQuery, nodes []*BundleReleaseOverridesV2, init func(*BundleReleaseOverridesV2), assign func(*BundleReleaseOverridesV2, *Bundles)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleReleaseOverridesV2)
	for i := range nodes {
		fk := nodes[i].BundleId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundles.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "bundleId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BundleReleaseOverridesV2Query) loadRelease(ctx context.Context, query *BundleReleasesQuery, nodes []*BundleReleaseOverridesV2, init func(*BundleReleaseOverridesV2), assign func(*BundleReleaseOverridesV2, *BundleReleases)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleReleaseOverridesV2)
	for i := range nodes {
		fk := nodes[i].ReleaseId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundlereleases.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "releaseId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BundleReleaseOverridesV2Query) loadCreatedBy(ctx context.Context, query *UserQuery, nodes []*BundleReleaseOverridesV2, init func(*BundleReleaseOverridesV2), assign func(*BundleReleaseOverridesV2, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleReleaseOverridesV2)
	for i := range nodes {
		fk := nodes[i].CreatedById
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "createdById" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BundleReleaseOverridesV2Query) loadApprovals(ctx context.Context, query *BundleReleaseOverrideApprovalsQuery, nodes []*BundleReleaseOverridesV2, init func(*BundleReleaseOverridesV2), assign func(*BundleReleaseOverridesV2, *BundleReleaseOverrideApprovals)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleaseOverridesV2)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundlereleaseoverrideapprovals.FieldOverrideId)
	}
	query.Where(predicate.BundleReleaseOverrideApprovals(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleaseoverridesv2.ApprovalsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.OverrideId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "overrideId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *BundleReleaseOverridesV2Query) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *BundleReleaseOverridesV2Query) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(bundlereleaseoverridesv2.Table, bundlereleaseoverridesv2.Columns, sqlgraph.NewFieldSpec(bundlereleaseoverridesv2.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bundlereleaseoverridesv2.FieldID)
		for i := range fields {
			if fields[i] != bundlereleaseoverridesv2.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withBundle != nil {
			_spec.Node.AddColumnOnce(bundlereleaseoverridesv2.FieldBundleId)
		}
		if _q.withRelease != nil {
			_spec.Node.AddColumnOnce(bundlereleaseoverridesv2.FieldReleaseId)
		}
		if _q.withCreatedBy != nil {
			_spec.Node.AddColumnOnce(bundlereleaseoverridesv2.FieldCreatedById)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *BundleReleaseOverridesV2Query) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(bundlereleaseoverridesv2.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = bundlereleaseoverridesv2.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *BundleReleaseOverridesV2Query) ForUpdate(opts ...sql.LockOption) *BundleReleaseOverridesV2Query {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *BundleReleaseOverridesV2Query) ForShare(opts ...sql.LockOption) *BundleReleaseOverridesV2Query {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *BundleReleaseOverridesV2Query) Modify(modifiers ...func(s *sql.Selector)) *BundleReleaseOverridesV2Select {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// BundleReleaseOverridesV2GroupBy is the group-by builder for BundleReleaseOverridesV2 entities.
type BundleReleaseOverridesV2GroupBy struct {
	selector
	build *BundleReleaseOverridesV2Query
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *BundleReleaseOverridesV2GroupBy) Aggregate(fns ...AggregateFunc) *BundleReleaseOverridesV2GroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *BundleReleaseOverridesV2GroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BundleReleaseOverridesV2Query, *BundleReleaseOverridesV2GroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *BundleReleaseOverridesV2GroupBy) sqlScan(ctx context.Context, root *BundleReleaseOverridesV2Query, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BundleReleaseOverridesV2Select is the builder for selecting fields of BundleReleaseOverridesV2 entities.
type BundleReleaseOverridesV2Select struct {
	*BundleReleaseOverridesV2Query
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *BundleReleaseOverridesV2Select) Aggregate(fns ...AggregateFunc) *BundleReleaseOverridesV2Select {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *BundleReleaseOverridesV2Select) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BundleReleaseOverridesV2Query, *BundleReleaseOverridesV2Select](ctx, _s.BundleReleaseOverridesV2Query, _s, _s.inters, v)
}

func (_s *BundleReleaseOverridesV2Select) sqlScan(ctx context.Context, root *BundleReleaseOverridesV2Query, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *BundleReleaseOverridesV2Select) Modify(modifiers ...func(s *sql.Selector)) *BundleReleaseOverridesV2Select {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
