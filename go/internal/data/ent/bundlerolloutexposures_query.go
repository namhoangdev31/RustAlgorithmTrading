// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"trading/control-gateway/internal/data/ent/bundlereleasetracks"
	"trading/control-gateway/internal/data/ent/bundlerolloutexposures"
	"trading/control-gateway/internal/data/ent/bundlerollouts"
	"trading/control-gateway/internal/data/ent/bundles"
	"trading/control-gateway/internal/data/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// BundleRolloutExposuresQuery is the builder for querying BundleRolloutExposures entities.
type BundleRolloutExposuresQuery struct {
	config
	ctx         *QueryContext
	order       []bundlerolloutexposures.OrderOption
	inters      []Interceptor
	predicates  []predicate.BundleRolloutExposures
	withRollout *BundleRolloutsQuery
	withBundle  *BundlesQuery
	withTrack   *BundleReleaseTracksQuery
	modifiers   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BundleRolloutExposuresQuery builder.
func (_q *BundleRolloutExposuresQuery) Where(ps ...predicate.BundleRolloutExposures) *BundleRolloutExposuresQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *BundleRolloutExposuresQuery) Limit(limit int) *BundleRolloutExposuresQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *BundleRolloutExposuresQuery) Offset(offset int) *BundleRolloutExposuresQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *BundleRolloutExposuresQuery) Unique(unique bool) *BundleRolloutExposuresQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *BundleRolloutExposuresQuery) Order(o ...bundlerolloutexposures.OrderOption) *BundleRolloutExposuresQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryRollout chains the current query on the "rollout" edge.
func (_q *BundleRolloutExposuresQuery) QueryRollout() *BundleRolloutsQuery {
	query := (&BundleRolloutsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlerolloutexposures.Table, bundlerolloutexposures.FieldID, selector),
			sqlgraph.To(bundlerollouts.Table, bundlerollouts.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlerolloutexposures.RolloutTable, bundlerolloutexposures.RolloutColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBundle chains the current query on the "bundle" edge.
func (_q *BundleRolloutExposuresQuery) QueryBundle() *BundlesQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlerolloutexposures.Table, bundlerolloutexposures.FieldID, selector),
			sqlgraph.To(bundles.Table, bundles.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlerolloutexposures.BundleTable, bundlerolloutexposures.BundleColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTrack chains the current query on the "track" edge.
func (_q *BundleRolloutExposuresQuery) QueryTrack() *BundleReleaseTracksQuery {
	query := (&BundleReleaseTracksClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlerolloutexposures.Table, bundlerolloutexposures.FieldID, selector),
			sqlgraph.To(bundlereleasetracks.Table, bundlereleasetracks.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlerolloutexposures.TrackTable, bundlerolloutexposures.TrackColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BundleRolloutExposures entity from the query.
// Returns a *NotFoundError when no BundleRolloutExposures was found.
func (_q *BundleRolloutExposuresQuery) First(ctx context.Context) (*BundleRolloutExposures, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bundlerolloutexposures.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *BundleRolloutExposuresQuery) FirstX(ctx context.Context) *BundleRolloutExposures {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BundleRolloutExposures ID from the query.
// Returns a *NotFoundError when no BundleRolloutExposures ID was found.
func (_q *BundleRolloutExposuresQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bundlerolloutexposures.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *BundleRolloutExposuresQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BundleRolloutExposures entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BundleRolloutExposures entity is found.
// Returns a *NotFoundError when no BundleRolloutExposures entities are found.
func (_q *BundleRolloutExposuresQuery) Only(ctx context.Context) (*BundleRolloutExposures, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bundlerolloutexposures.Label}
	default:
		return nil, &NotSingularError{bundlerolloutexposures.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *BundleRolloutExposuresQuery) OnlyX(ctx context.Context) *BundleRolloutExposures {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BundleRolloutExposures ID in the query.
// Returns a *NotSingularError when more than one BundleRolloutExposures ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *BundleRolloutExposuresQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bundlerolloutexposures.Label}
	default:
		err = &NotSingularError{bundlerolloutexposures.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *BundleRolloutExposuresQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BundleRolloutExposuresSlice.
func (_q *BundleRolloutExposuresQuery) All(ctx context.Context) ([]*BundleRolloutExposures, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BundleRolloutExposures, *BundleRolloutExposuresQuery]()
	return withInterceptors[[]*BundleRolloutExposures](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *BundleRolloutExposuresQuery) AllX(ctx context.Context) []*BundleRolloutExposures {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BundleRolloutExposures IDs.
func (_q *BundleRolloutExposuresQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(bundlerolloutexposures.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *BundleRolloutExposuresQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *BundleRolloutExposuresQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*BundleRolloutExposuresQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *BundleRolloutExposuresQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *BundleRolloutExposuresQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *BundleRolloutExposuresQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BundleRolloutExposuresQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *BundleRolloutExposuresQuery) Clone() *BundleRolloutExposuresQuery {
	if _q == nil {
		return nil
	}
	return &BundleRolloutExposuresQuery{
		config:      _q.config,
		ctx:         _q.ctx.Clone(),
		order:       append([]bundlerolloutexposures.OrderOption{}, _q.order...),
		inters:      append([]Interceptor{}, _q.inters...),
		predicates:  append([]predicate.BundleRolloutExposures{}, _q.predicates...),
		withRollout: _q.withRollout.Clone(),
		withBundle:  _q.withBundle.Clone(),
		withTrack:   _q.withTrack.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithRollout tells the query-builder to eager-load the nodes that are connected to
// the "rollout" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleRolloutExposuresQuery) WithRollout(opts ...func(*BundleRolloutsQuery)) *BundleRolloutExposuresQuery {
	query := (&BundleRolloutsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRollout = query
	return _q
}

// WithBundle tells the query-builder to eager-load the nodes that are connected to
// the "bundle" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleRolloutExposuresQuery) WithBundle(opts ...func(*BundlesQuery)) *BundleRolloutExposuresQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBundle = query
	return _q
}

// WithTrack tells the query-builder to eager-load the nodes that are connected to
// the "track" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleRolloutExposuresQuery) WithTrack(opts ...func(*BundleReleaseTracksQuery)) *BundleRolloutExposuresQuery {
	query := (&BundleReleaseTracksClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTrack = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RolloutId uuid.UUID `json:"rolloutId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BundleRolloutExposures.Query().
//		GroupBy(bundlerolloutexposures.FieldRolloutId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *BundleRolloutExposuresQuery) GroupBy(field string, fields ...string) *BundleRolloutExposuresGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BundleRolloutExposuresGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = bundlerolloutexposures.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RolloutId uuid.UUID `json:"rolloutId,omitempty"`
//	}
//
//	client.BundleRolloutExposures.Query().
//		Select(bundlerolloutexposures.FieldRolloutId).
//		Scan(ctx, &v)
func (_q *BundleRolloutExposuresQuery) Select(fields ...string) *BundleRolloutExposuresSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &BundleRolloutExposuresSelect{BundleRolloutExposuresQuery: _q}
	sbuild.label = bundlerolloutexposures.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BundleRolloutExposuresSelect configured with the given aggregations.
func (_q *BundleRolloutExposuresQuery) Aggregate(fns ...AggregateFunc) *BundleRolloutExposuresSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *BundleRolloutExposuresQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !bundlerolloutexposures.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *BundleRolloutExposuresQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BundleRolloutExposures, error) {
	var (
		nodes       = []*BundleRolloutExposures{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withRollout != nil,
			_q.withBundle != nil,
			_q.withTrack != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BundleRolloutExposures).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BundleRolloutExposures{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withRollout; query != nil {
		if err := _q.loadRollout(ctx, query, nodes, nil,
			func(n *BundleRolloutExposures, e *BundleRollouts) { n.Edges.Rollout = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withBundle; query != nil {
		if err := _q.loadBundle(ctx, query, nodes, nil,
			func(n *BundleRolloutExposures, e *Bundles) { n.Edges.Bundle = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTrack; query != nil {
		if err := _q.loadTrack(ctx, query, nodes, nil,
			func(n *BundleRolloutExposures, e *BundleReleaseTracks) { n.Edges.Track = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *BundleRolloutExposuresQuery) loadRollout(ctx context.Context, query *BundleRolloutsQuery, nodes []*BundleRolloutExposures, init func(*BundleRolloutExposures), assign func(*BundleRolloutExposures, *BundleRollouts)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleRolloutExposures)
	for i := range nodes {
		fk := nodes[i].RolloutId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundlerollouts.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "rolloutId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BundleRolloutExposuresQuery) loadBundle(ctx context.Context, query *BundlesQuery, nodes []*BundleRolloutExposures, init func(*BundleRolloutExposures), assign func(*BundleRolloutExposures, *Bundles)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleRolloutExposures)
	for i := range nodes {
		fk := nodes[i].BundleId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundles.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "bundleId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BundleRolloutExposuresQuery) loadTrack(ctx context.Context, query *BundleReleaseTracksQuery, nodes []*BundleRolloutExposures, init func(*BundleRolloutExposures), assign func(*BundleRolloutExposures, *BundleReleaseTracks)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleRolloutExposures)
	for i := range nodes {
		fk := nodes[i].TrackId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundlereleasetracks.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "trackId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *BundleRolloutExposuresQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *BundleRolloutExposuresQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(bundlerolloutexposures.Table, bundlerolloutexposures.Columns, sqlgraph.NewFieldSpec(bundlerolloutexposures.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bundlerolloutexposures.FieldID)
		for i := range fields {
			if fields[i] != bundlerolloutexposures.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withRollout != nil {
			_spec.Node.AddColumnOnce(bundlerolloutexposures.FieldRolloutId)
		}
		if _q.withBundle != nil {
			_spec.Node.AddColumnOnce(bundlerolloutexposures.FieldBundleId)
		}
		if _q.withTrack != nil {
			_spec.Node.AddColumnOnce(bundlerolloutexposures.FieldTrackId)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *BundleRolloutExposuresQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(bundlerolloutexposures.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = bundlerolloutexposures.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *BundleRolloutExposuresQuery) ForUpdate(opts ...sql.LockOption) *BundleRolloutExposuresQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *BundleRolloutExposuresQuery) ForShare(opts ...sql.LockOption) *BundleRolloutExposuresQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *BundleRolloutExposuresQuery) Modify(modifiers ...func(s *sql.Selector)) *BundleRolloutExposuresSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// BundleRolloutExposuresGroupBy is the group-by builder for BundleRolloutExposures entities.
type BundleRolloutExposuresGroupBy struct {
	selector
	build *BundleRolloutExposuresQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *BundleRolloutExposuresGroupBy) Aggregate(fns ...AggregateFunc) *BundleRolloutExposuresGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *BundleRolloutExposuresGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BundleRolloutExposuresQuery, *BundleRolloutExposuresGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *BundleRolloutExposuresGroupBy) sqlScan(ctx context.Context, root *BundleRolloutExposuresQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BundleRolloutExposuresSelect is the builder for selecting fields of BundleRolloutExposures entities.
type BundleRolloutExposuresSelect struct {
	*BundleRolloutExposuresQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *BundleRolloutExposuresSelect) Aggregate(fns ...AggregateFunc) *BundleRolloutExposuresSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *BundleRolloutExposuresSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BundleRolloutExposuresQuery, *BundleRolloutExposuresSelect](ctx, _s.BundleRolloutExposuresQuery, _s, _s.inters, v)
}

func (_s *BundleRolloutExposuresSelect) sqlScan(ctx context.Context, root *BundleRolloutExposuresQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *BundleRolloutExposuresSelect) Modify(modifiers ...func(s *sql.Selector)) *BundleRolloutExposuresSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
