// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"trading/control-gateway/internal/data/ent/bundles"
	"trading/control-gateway/internal/data/ent/bundlewebhookdeliveries"
	"trading/control-gateway/internal/data/ent/bundlewebhooks"
	"trading/control-gateway/internal/data/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// BundleWebhooksQuery is the builder for querying BundleWebhooks entities.
type BundleWebhooksQuery struct {
	config
	ctx            *QueryContext
	order          []bundlewebhooks.OrderOption
	inters         []Interceptor
	predicates     []predicate.BundleWebhooks
	withBundle     *BundlesQuery
	withDeliveries *BundleWebhookDeliveriesQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BundleWebhooksQuery builder.
func (_q *BundleWebhooksQuery) Where(ps ...predicate.BundleWebhooks) *BundleWebhooksQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *BundleWebhooksQuery) Limit(limit int) *BundleWebhooksQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *BundleWebhooksQuery) Offset(offset int) *BundleWebhooksQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *BundleWebhooksQuery) Unique(unique bool) *BundleWebhooksQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *BundleWebhooksQuery) Order(o ...bundlewebhooks.OrderOption) *BundleWebhooksQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryBundle chains the current query on the "bundle" edge.
func (_q *BundleWebhooksQuery) QueryBundle() *BundlesQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlewebhooks.Table, bundlewebhooks.FieldID, selector),
			sqlgraph.To(bundles.Table, bundles.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlewebhooks.BundleTable, bundlewebhooks.BundleColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDeliveries chains the current query on the "deliveries" edge.
func (_q *BundleWebhooksQuery) QueryDeliveries() *BundleWebhookDeliveriesQuery {
	query := (&BundleWebhookDeliveriesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlewebhooks.Table, bundlewebhooks.FieldID, selector),
			sqlgraph.To(bundlewebhookdeliveries.Table, bundlewebhookdeliveries.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlewebhooks.DeliveriesTable, bundlewebhooks.DeliveriesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BundleWebhooks entity from the query.
// Returns a *NotFoundError when no BundleWebhooks was found.
func (_q *BundleWebhooksQuery) First(ctx context.Context) (*BundleWebhooks, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bundlewebhooks.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *BundleWebhooksQuery) FirstX(ctx context.Context) *BundleWebhooks {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BundleWebhooks ID from the query.
// Returns a *NotFoundError when no BundleWebhooks ID was found.
func (_q *BundleWebhooksQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bundlewebhooks.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *BundleWebhooksQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BundleWebhooks entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BundleWebhooks entity is found.
// Returns a *NotFoundError when no BundleWebhooks entities are found.
func (_q *BundleWebhooksQuery) Only(ctx context.Context) (*BundleWebhooks, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bundlewebhooks.Label}
	default:
		return nil, &NotSingularError{bundlewebhooks.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *BundleWebhooksQuery) OnlyX(ctx context.Context) *BundleWebhooks {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BundleWebhooks ID in the query.
// Returns a *NotSingularError when more than one BundleWebhooks ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *BundleWebhooksQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bundlewebhooks.Label}
	default:
		err = &NotSingularError{bundlewebhooks.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *BundleWebhooksQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BundleWebhooksSlice.
func (_q *BundleWebhooksQuery) All(ctx context.Context) ([]*BundleWebhooks, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BundleWebhooks, *BundleWebhooksQuery]()
	return withInterceptors[[]*BundleWebhooks](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *BundleWebhooksQuery) AllX(ctx context.Context) []*BundleWebhooks {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BundleWebhooks IDs.
func (_q *BundleWebhooksQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(bundlewebhooks.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *BundleWebhooksQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *BundleWebhooksQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*BundleWebhooksQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *BundleWebhooksQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *BundleWebhooksQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *BundleWebhooksQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BundleWebhooksQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *BundleWebhooksQuery) Clone() *BundleWebhooksQuery {
	if _q == nil {
		return nil
	}
	return &BundleWebhooksQuery{
		config:         _q.config,
		ctx:            _q.ctx.Clone(),
		order:          append([]bundlewebhooks.OrderOption{}, _q.order...),
		inters:         append([]Interceptor{}, _q.inters...),
		predicates:     append([]predicate.BundleWebhooks{}, _q.predicates...),
		withBundle:     _q.withBundle.Clone(),
		withDeliveries: _q.withDeliveries.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithBundle tells the query-builder to eager-load the nodes that are connected to
// the "bundle" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleWebhooksQuery) WithBundle(opts ...func(*BundlesQuery)) *BundleWebhooksQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBundle = query
	return _q
}

// WithDeliveries tells the query-builder to eager-load the nodes that are connected to
// the "deliveries" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleWebhooksQuery) WithDeliveries(opts ...func(*BundleWebhookDeliveriesQuery)) *BundleWebhooksQuery {
	query := (&BundleWebhookDeliveriesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withDeliveries = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BundleId uuid.UUID `json:"bundleId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BundleWebhooks.Query().
//		GroupBy(bundlewebhooks.FieldBundleId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *BundleWebhooksQuery) GroupBy(field string, fields ...string) *BundleWebhooksGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BundleWebhooksGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = bundlewebhooks.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BundleId uuid.UUID `json:"bundleId,omitempty"`
//	}
//
//	client.BundleWebhooks.Query().
//		Select(bundlewebhooks.FieldBundleId).
//		Scan(ctx, &v)
func (_q *BundleWebhooksQuery) Select(fields ...string) *BundleWebhooksSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &BundleWebhooksSelect{BundleWebhooksQuery: _q}
	sbuild.label = bundlewebhooks.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BundleWebhooksSelect configured with the given aggregations.
func (_q *BundleWebhooksQuery) Aggregate(fns ...AggregateFunc) *BundleWebhooksSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *BundleWebhooksQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !bundlewebhooks.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *BundleWebhooksQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BundleWebhooks, error) {
	var (
		nodes       = []*BundleWebhooks{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withBundle != nil,
			_q.withDeliveries != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BundleWebhooks).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BundleWebhooks{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withBundle; query != nil {
		if err := _q.loadBundle(ctx, query, nodes, nil,
			func(n *BundleWebhooks, e *Bundles) { n.Edges.Bundle = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withDeliveries; query != nil {
		if err := _q.loadDeliveries(ctx, query, nodes,
			func(n *BundleWebhooks) { n.Edges.Deliveries = []*BundleWebhookDeliveries{} },
			func(n *BundleWebhooks, e *BundleWebhookDeliveries) {
				n.Edges.Deliveries = append(n.Edges.Deliveries, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *BundleWebhooksQuery) loadBundle(ctx context.Context, query *BundlesQuery, nodes []*BundleWebhooks, init func(*BundleWebhooks), assign func(*BundleWebhooks, *Bundles)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleWebhooks)
	for i := range nodes {
		fk := nodes[i].BundleId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundles.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "bundleId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BundleWebhooksQuery) loadDeliveries(ctx context.Context, query *BundleWebhookDeliveriesQuery, nodes []*BundleWebhooks, init func(*BundleWebhooks), assign func(*BundleWebhooks, *BundleWebhookDeliveries)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleWebhooks)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundlewebhookdeliveries.FieldWebhookId)
	}
	query.Where(predicate.BundleWebhookDeliveries(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlewebhooks.DeliveriesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.WebhookId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "webhookId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *BundleWebhooksQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *BundleWebhooksQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(bundlewebhooks.Table, bundlewebhooks.Columns, sqlgraph.NewFieldSpec(bundlewebhooks.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bundlewebhooks.FieldID)
		for i := range fields {
			if fields[i] != bundlewebhooks.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withBundle != nil {
			_spec.Node.AddColumnOnce(bundlewebhooks.FieldBundleId)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *BundleWebhooksQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(bundlewebhooks.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = bundlewebhooks.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *BundleWebhooksQuery) ForUpdate(opts ...sql.LockOption) *BundleWebhooksQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *BundleWebhooksQuery) ForShare(opts ...sql.LockOption) *BundleWebhooksQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *BundleWebhooksQuery) Modify(modifiers ...func(s *sql.Selector)) *BundleWebhooksSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// BundleWebhooksGroupBy is the group-by builder for BundleWebhooks entities.
type BundleWebhooksGroupBy struct {
	selector
	build *BundleWebhooksQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *BundleWebhooksGroupBy) Aggregate(fns ...AggregateFunc) *BundleWebhooksGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *BundleWebhooksGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BundleWebhooksQuery, *BundleWebhooksGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *BundleWebhooksGroupBy) sqlScan(ctx context.Context, root *BundleWebhooksQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BundleWebhooksSelect is the builder for selecting fields of BundleWebhooks entities.
type BundleWebhooksSelect struct {
	*BundleWebhooksQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *BundleWebhooksSelect) Aggregate(fns ...AggregateFunc) *BundleWebhooksSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *BundleWebhooksSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BundleWebhooksQuery, *BundleWebhooksSelect](ctx, _s.BundleWebhooksQuery, _s, _s.inters, v)
}

func (_s *BundleWebhooksSelect) sqlScan(ctx context.Context, root *BundleWebhooksQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *BundleWebhooksSelect) Modify(modifiers ...func(s *sql.Selector)) *BundleWebhooksSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
