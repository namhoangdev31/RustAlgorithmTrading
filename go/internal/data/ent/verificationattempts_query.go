// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"trading/control-gateway/internal/data/ent/predicate"
	"trading/control-gateway/internal/data/ent/verificationattempts"
	"trading/control-gateway/internal/data/ent/verificationevidence"
	"trading/control-gateway/internal/data/ent/verificationfindings"
	"trading/control-gateway/internal/data/ent/verificationtasks"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// VerificationAttemptsQuery is the builder for querying VerificationAttempts entities.
type VerificationAttemptsQuery struct {
	config
	ctx                 *QueryContext
	order               []verificationattempts.OrderOption
	inters              []Interceptor
	predicates          []predicate.VerificationAttempts
	withTask            *VerificationTasksQuery
	withAcceptedForTask *VerificationTasksQuery
	withFindings        *VerificationFindingsQuery
	withEvidence        *VerificationEvidenceQuery
	modifiers           []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VerificationAttemptsQuery builder.
func (_q *VerificationAttemptsQuery) Where(ps ...predicate.VerificationAttempts) *VerificationAttemptsQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *VerificationAttemptsQuery) Limit(limit int) *VerificationAttemptsQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *VerificationAttemptsQuery) Offset(offset int) *VerificationAttemptsQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *VerificationAttemptsQuery) Unique(unique bool) *VerificationAttemptsQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *VerificationAttemptsQuery) Order(o ...verificationattempts.OrderOption) *VerificationAttemptsQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryTask chains the current query on the "task" edge.
func (_q *VerificationAttemptsQuery) QueryTask() *VerificationTasksQuery {
	query := (&VerificationTasksClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationattempts.Table, verificationattempts.FieldID, selector),
			sqlgraph.To(verificationtasks.Table, verificationtasks.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, verificationattempts.TaskTable, verificationattempts.TaskColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAcceptedForTask chains the current query on the "acceptedForTask" edge.
func (_q *VerificationAttemptsQuery) QueryAcceptedForTask() *VerificationTasksQuery {
	query := (&VerificationTasksClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationattempts.Table, verificationattempts.FieldID, selector),
			sqlgraph.To(verificationtasks.Table, verificationtasks.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, verificationattempts.AcceptedForTaskTable, verificationattempts.AcceptedForTaskColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFindings chains the current query on the "findings" edge.
func (_q *VerificationAttemptsQuery) QueryFindings() *VerificationFindingsQuery {
	query := (&VerificationFindingsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationattempts.Table, verificationattempts.FieldID, selector),
			sqlgraph.To(verificationfindings.Table, verificationfindings.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, verificationattempts.FindingsTable, verificationattempts.FindingsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEvidence chains the current query on the "evidence" edge.
func (_q *VerificationAttemptsQuery) QueryEvidence() *VerificationEvidenceQuery {
	query := (&VerificationEvidenceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(verificationattempts.Table, verificationattempts.FieldID, selector),
			sqlgraph.To(verificationevidence.Table, verificationevidence.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, verificationattempts.EvidenceTable, verificationattempts.EvidenceColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first VerificationAttempts entity from the query.
// Returns a *NotFoundError when no VerificationAttempts was found.
func (_q *VerificationAttemptsQuery) First(ctx context.Context) (*VerificationAttempts, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{verificationattempts.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *VerificationAttemptsQuery) FirstX(ctx context.Context) *VerificationAttempts {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first VerificationAttempts ID from the query.
// Returns a *NotFoundError when no VerificationAttempts ID was found.
func (_q *VerificationAttemptsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{verificationattempts.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *VerificationAttemptsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single VerificationAttempts entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one VerificationAttempts entity is found.
// Returns a *NotFoundError when no VerificationAttempts entities are found.
func (_q *VerificationAttemptsQuery) Only(ctx context.Context) (*VerificationAttempts, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{verificationattempts.Label}
	default:
		return nil, &NotSingularError{verificationattempts.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *VerificationAttemptsQuery) OnlyX(ctx context.Context) *VerificationAttempts {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only VerificationAttempts ID in the query.
// Returns a *NotSingularError when more than one VerificationAttempts ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *VerificationAttemptsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{verificationattempts.Label}
	default:
		err = &NotSingularError{verificationattempts.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *VerificationAttemptsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of VerificationAttemptsSlice.
func (_q *VerificationAttemptsQuery) All(ctx context.Context) ([]*VerificationAttempts, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*VerificationAttempts, *VerificationAttemptsQuery]()
	return withInterceptors[[]*VerificationAttempts](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *VerificationAttemptsQuery) AllX(ctx context.Context) []*VerificationAttempts {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of VerificationAttempts IDs.
func (_q *VerificationAttemptsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(verificationattempts.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *VerificationAttemptsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *VerificationAttemptsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*VerificationAttemptsQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *VerificationAttemptsQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *VerificationAttemptsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *VerificationAttemptsQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VerificationAttemptsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *VerificationAttemptsQuery) Clone() *VerificationAttemptsQuery {
	if _q == nil {
		return nil
	}
	return &VerificationAttemptsQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]verificationattempts.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.VerificationAttempts{}, _q.predicates...),
		withTask:            _q.withTask.Clone(),
		withAcceptedForTask: _q.withAcceptedForTask.Clone(),
		withFindings:        _q.withFindings.Clone(),
		withEvidence:        _q.withEvidence.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithTask tells the query-builder to eager-load the nodes that are connected to
// the "task" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationAttemptsQuery) WithTask(opts ...func(*VerificationTasksQuery)) *VerificationAttemptsQuery {
	query := (&VerificationTasksClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTask = query
	return _q
}

// WithAcceptedForTask tells the query-builder to eager-load the nodes that are connected to
// the "acceptedForTask" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationAttemptsQuery) WithAcceptedForTask(opts ...func(*VerificationTasksQuery)) *VerificationAttemptsQuery {
	query := (&VerificationTasksClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAcceptedForTask = query
	return _q
}

// WithFindings tells the query-builder to eager-load the nodes that are connected to
// the "findings" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationAttemptsQuery) WithFindings(opts ...func(*VerificationFindingsQuery)) *VerificationAttemptsQuery {
	query := (&VerificationFindingsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withFindings = query
	return _q
}

// WithEvidence tells the query-builder to eager-load the nodes that are connected to
// the "evidence" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *VerificationAttemptsQuery) WithEvidence(opts ...func(*VerificationEvidenceQuery)) *VerificationAttemptsQuery {
	query := (&VerificationEvidenceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEvidence = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TaskId uuid.UUID `json:"taskId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.VerificationAttempts.Query().
//		GroupBy(verificationattempts.FieldTaskId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *VerificationAttemptsQuery) GroupBy(field string, fields ...string) *VerificationAttemptsGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &VerificationAttemptsGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = verificationattempts.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TaskId uuid.UUID `json:"taskId,omitempty"`
//	}
//
//	client.VerificationAttempts.Query().
//		Select(verificationattempts.FieldTaskId).
//		Scan(ctx, &v)
func (_q *VerificationAttemptsQuery) Select(fields ...string) *VerificationAttemptsSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &VerificationAttemptsSelect{VerificationAttemptsQuery: _q}
	sbuild.label = verificationattempts.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a VerificationAttemptsSelect configured with the given aggregations.
func (_q *VerificationAttemptsQuery) Aggregate(fns ...AggregateFunc) *VerificationAttemptsSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *VerificationAttemptsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !verificationattempts.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *VerificationAttemptsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*VerificationAttempts, error) {
	var (
		nodes       = []*VerificationAttempts{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withTask != nil,
			_q.withAcceptedForTask != nil,
			_q.withFindings != nil,
			_q.withEvidence != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*VerificationAttempts).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &VerificationAttempts{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withTask; query != nil {
		if err := _q.loadTask(ctx, query, nodes, nil,
			func(n *VerificationAttempts, e *VerificationTasks) { n.Edges.Task = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAcceptedForTask; query != nil {
		if err := _q.loadAcceptedForTask(ctx, query, nodes, nil,
			func(n *VerificationAttempts, e *VerificationTasks) { n.Edges.AcceptedForTask = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withFindings; query != nil {
		if err := _q.loadFindings(ctx, query, nodes,
			func(n *VerificationAttempts) { n.Edges.Findings = []*VerificationFindings{} },
			func(n *VerificationAttempts, e *VerificationFindings) { n.Edges.Findings = append(n.Edges.Findings, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEvidence; query != nil {
		if err := _q.loadEvidence(ctx, query, nodes,
			func(n *VerificationAttempts) { n.Edges.Evidence = []*VerificationEvidence{} },
			func(n *VerificationAttempts, e *VerificationEvidence) { n.Edges.Evidence = append(n.Edges.Evidence, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *VerificationAttemptsQuery) loadTask(ctx context.Context, query *VerificationTasksQuery, nodes []*VerificationAttempts, init func(*VerificationAttempts), assign func(*VerificationAttempts, *VerificationTasks)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*VerificationAttempts)
	for i := range nodes {
		fk := nodes[i].TaskId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(verificationtasks.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "taskId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *VerificationAttemptsQuery) loadAcceptedForTask(ctx context.Context, query *VerificationTasksQuery, nodes []*VerificationAttempts, init func(*VerificationAttempts), assign func(*VerificationAttempts, *VerificationTasks)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationAttempts)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(verificationtasks.FieldAcceptedAttemptId)
	}
	query.Where(predicate.VerificationTasks(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationattempts.AcceptedForTaskColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AcceptedAttemptId
		if fk == nil {
			return fmt.Errorf(`foreign-key "acceptedAttemptId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "acceptedAttemptId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *VerificationAttemptsQuery) loadFindings(ctx context.Context, query *VerificationFindingsQuery, nodes []*VerificationAttempts, init func(*VerificationAttempts), assign func(*VerificationAttempts, *VerificationFindings)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationAttempts)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(verificationfindings.FieldAttemptId)
	}
	query.Where(predicate.VerificationFindings(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationattempts.FindingsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AttemptId
		if fk == nil {
			return fmt.Errorf(`foreign-key "attemptId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "attemptId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *VerificationAttemptsQuery) loadEvidence(ctx context.Context, query *VerificationEvidenceQuery, nodes []*VerificationAttempts, init func(*VerificationAttempts), assign func(*VerificationAttempts, *VerificationEvidence)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*VerificationAttempts)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(verificationevidence.FieldAttemptId)
	}
	query.Where(predicate.VerificationEvidence(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(verificationattempts.EvidenceColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AttemptId
		if fk == nil {
			return fmt.Errorf(`foreign-key "attemptId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "attemptId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *VerificationAttemptsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *VerificationAttemptsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(verificationattempts.Table, verificationattempts.Columns, sqlgraph.NewFieldSpec(verificationattempts.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, verificationattempts.FieldID)
		for i := range fields {
			if fields[i] != verificationattempts.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withTask != nil {
			_spec.Node.AddColumnOnce(verificationattempts.FieldTaskId)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *VerificationAttemptsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(verificationattempts.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = verificationattempts.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *VerificationAttemptsQuery) ForUpdate(opts ...sql.LockOption) *VerificationAttemptsQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *VerificationAttemptsQuery) ForShare(opts ...sql.LockOption) *VerificationAttemptsQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *VerificationAttemptsQuery) Modify(modifiers ...func(s *sql.Selector)) *VerificationAttemptsSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// VerificationAttemptsGroupBy is the group-by builder for VerificationAttempts entities.
type VerificationAttemptsGroupBy struct {
	selector
	build *VerificationAttemptsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *VerificationAttemptsGroupBy) Aggregate(fns ...AggregateFunc) *VerificationAttemptsGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *VerificationAttemptsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VerificationAttemptsQuery, *VerificationAttemptsGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *VerificationAttemptsGroupBy) sqlScan(ctx context.Context, root *VerificationAttemptsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// VerificationAttemptsSelect is the builder for selecting fields of VerificationAttempts entities.
type VerificationAttemptsSelect struct {
	*VerificationAttemptsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *VerificationAttemptsSelect) Aggregate(fns ...AggregateFunc) *VerificationAttemptsSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *VerificationAttemptsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VerificationAttemptsQuery, *VerificationAttemptsSelect](ctx, _s.VerificationAttemptsQuery, _s, _s.inters, v)
}

func (_s *VerificationAttemptsSelect) sqlScan(ctx context.Context, root *VerificationAttemptsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *VerificationAttemptsSelect) Modify(modifiers ...func(s *sql.Selector)) *VerificationAttemptsSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
