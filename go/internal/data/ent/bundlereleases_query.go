// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"trading/control-gateway/internal/data/ent/bundleabtestexposures"
	"trading/control-gateway/internal/data/ent/bundleabtests"
	"trading/control-gateway/internal/data/ent/bundleartifacts"
	"trading/control-gateway/internal/data/ent/bundlebuildjobs"
	"trading/control-gateway/internal/data/ent/bundlechannels"
	"trading/control-gateway/internal/data/ent/bundlecrashevents"
	"trading/control-gateway/internal/data/ent/bundledeliveryrolloutexposures"
	"trading/control-gateway/internal/data/ent/bundledeliveryrollouts"
	"trading/control-gateway/internal/data/ent/bundlereleaseapprovals"
	"trading/control-gateway/internal/data/ent/bundlereleaseoverridesv2"
	"trading/control-gateway/internal/data/ent/bundlereleases"
	"trading/control-gateway/internal/data/ent/bundlereviewqueue"
	"trading/control-gateway/internal/data/ent/bundles"
	"trading/control-gateway/internal/data/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// BundleReleasesQuery is the builder for querying BundleReleases entities.
type BundleReleasesQuery struct {
	config
	ctx                       *QueryContext
	order                     []bundlereleases.OrderOption
	inters                    []Interceptor
	predicates                []predicate.BundleReleases
	withBundle                *BundlesQuery
	withChannel               *BundleChannelsQuery
	withCurrentForChannels    *BundleChannelsQuery
	withArtifacts             *BundleArtifactsQuery
	withBuildJobs             *BundleBuildJobsQuery
	withApprovals             *BundleReleaseApprovalsQuery
	withOverrides             *BundleReleaseOverridesV2Query
	withBaselineRollouts      *BundleDeliveryRolloutsQuery
	withCandidateRollouts     *BundleDeliveryRolloutsQuery
	withRolloutExposures      *BundleDeliveryRolloutExposuresQuery
	withControlExperiments    *BundleAbTestsQuery
	withTreatmentExperiments  *BundleAbTestsQuery
	withReviewQueue           *BundleReviewQueueQuery
	withCrashEvents           *BundleCrashEventsQuery
	withBundleAbTestExposures *BundleAbTestExposuresQuery
	modifiers                 []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BundleReleasesQuery builder.
func (_q *BundleReleasesQuery) Where(ps ...predicate.BundleReleases) *BundleReleasesQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *BundleReleasesQuery) Limit(limit int) *BundleReleasesQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *BundleReleasesQuery) Offset(offset int) *BundleReleasesQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *BundleReleasesQuery) Unique(unique bool) *BundleReleasesQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *BundleReleasesQuery) Order(o ...bundlereleases.OrderOption) *BundleReleasesQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryBundle chains the current query on the "bundle" edge.
func (_q *BundleReleasesQuery) QueryBundle() *BundlesQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundles.Table, bundles.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlereleases.BundleTable, bundlereleases.BundleColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChannel chains the current query on the "channel" edge.
func (_q *BundleReleasesQuery) QueryChannel() *BundleChannelsQuery {
	query := (&BundleChannelsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundlechannels.Table, bundlechannels.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, bundlereleases.ChannelTable, bundlereleases.ChannelColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCurrentForChannels chains the current query on the "currentForChannels" edge.
func (_q *BundleReleasesQuery) QueryCurrentForChannels() *BundleChannelsQuery {
	query := (&BundleChannelsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundlechannels.Table, bundlechannels.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.CurrentForChannelsTable, bundlereleases.CurrentForChannelsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryArtifacts chains the current query on the "artifacts" edge.
func (_q *BundleReleasesQuery) QueryArtifacts() *BundleArtifactsQuery {
	query := (&BundleArtifactsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundleartifacts.Table, bundleartifacts.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.ArtifactsTable, bundlereleases.ArtifactsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBuildJobs chains the current query on the "buildJobs" edge.
func (_q *BundleReleasesQuery) QueryBuildJobs() *BundleBuildJobsQuery {
	query := (&BundleBuildJobsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundlebuildjobs.Table, bundlebuildjobs.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.BuildJobsTable, bundlereleases.BuildJobsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryApprovals chains the current query on the "approvals" edge.
func (_q *BundleReleasesQuery) QueryApprovals() *BundleReleaseApprovalsQuery {
	query := (&BundleReleaseApprovalsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundlereleaseapprovals.Table, bundlereleaseapprovals.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.ApprovalsTable, bundlereleases.ApprovalsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOverrides chains the current query on the "overrides" edge.
func (_q *BundleReleasesQuery) QueryOverrides() *BundleReleaseOverridesV2Query {
	query := (&BundleReleaseOverridesV2Client{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundlereleaseoverridesv2.Table, bundlereleaseoverridesv2.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.OverridesTable, bundlereleases.OverridesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBaselineRollouts chains the current query on the "baselineRollouts" edge.
func (_q *BundleReleasesQuery) QueryBaselineRollouts() *BundleDeliveryRolloutsQuery {
	query := (&BundleDeliveryRolloutsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundledeliveryrollouts.Table, bundledeliveryrollouts.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.BaselineRolloutsTable, bundlereleases.BaselineRolloutsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCandidateRollouts chains the current query on the "candidateRollouts" edge.
func (_q *BundleReleasesQuery) QueryCandidateRollouts() *BundleDeliveryRolloutsQuery {
	query := (&BundleDeliveryRolloutsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundledeliveryrollouts.Table, bundledeliveryrollouts.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.CandidateRolloutsTable, bundlereleases.CandidateRolloutsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRolloutExposures chains the current query on the "rolloutExposures" edge.
func (_q *BundleReleasesQuery) QueryRolloutExposures() *BundleDeliveryRolloutExposuresQuery {
	query := (&BundleDeliveryRolloutExposuresClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundledeliveryrolloutexposures.Table, bundledeliveryrolloutexposures.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.RolloutExposuresTable, bundlereleases.RolloutExposuresColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryControlExperiments chains the current query on the "controlExperiments" edge.
func (_q *BundleReleasesQuery) QueryControlExperiments() *BundleAbTestsQuery {
	query := (&BundleAbTestsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundleabtests.Table, bundleabtests.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.ControlExperimentsTable, bundlereleases.ControlExperimentsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTreatmentExperiments chains the current query on the "treatmentExperiments" edge.
func (_q *BundleReleasesQuery) QueryTreatmentExperiments() *BundleAbTestsQuery {
	query := (&BundleAbTestsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundleabtests.Table, bundleabtests.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.TreatmentExperimentsTable, bundlereleases.TreatmentExperimentsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReviewQueue chains the current query on the "reviewQueue" edge.
func (_q *BundleReleasesQuery) QueryReviewQueue() *BundleReviewQueueQuery {
	query := (&BundleReviewQueueClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundlereviewqueue.Table, bundlereviewqueue.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.ReviewQueueTable, bundlereleases.ReviewQueueColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCrashEvents chains the current query on the "crashEvents" edge.
func (_q *BundleReleasesQuery) QueryCrashEvents() *BundleCrashEventsQuery {
	query := (&BundleCrashEventsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundlecrashevents.Table, bundlecrashevents.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.CrashEventsTable, bundlereleases.CrashEventsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBundleAbTestExposures chains the current query on the "bundleAbTestExposures" edge.
func (_q *BundleReleasesQuery) QueryBundleAbTestExposures() *BundleAbTestExposuresQuery {
	query := (&BundleAbTestExposuresClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(bundlereleases.Table, bundlereleases.FieldID, selector),
			sqlgraph.To(bundleabtestexposures.Table, bundleabtestexposures.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, bundlereleases.BundleAbTestExposuresTable, bundlereleases.BundleAbTestExposuresColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BundleReleases entity from the query.
// Returns a *NotFoundError when no BundleReleases was found.
func (_q *BundleReleasesQuery) First(ctx context.Context) (*BundleReleases, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bundlereleases.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *BundleReleasesQuery) FirstX(ctx context.Context) *BundleReleases {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BundleReleases ID from the query.
// Returns a *NotFoundError when no BundleReleases ID was found.
func (_q *BundleReleasesQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bundlereleases.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *BundleReleasesQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BundleReleases entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BundleReleases entity is found.
// Returns a *NotFoundError when no BundleReleases entities are found.
func (_q *BundleReleasesQuery) Only(ctx context.Context) (*BundleReleases, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bundlereleases.Label}
	default:
		return nil, &NotSingularError{bundlereleases.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *BundleReleasesQuery) OnlyX(ctx context.Context) *BundleReleases {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BundleReleases ID in the query.
// Returns a *NotSingularError when more than one BundleReleases ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *BundleReleasesQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bundlereleases.Label}
	default:
		err = &NotSingularError{bundlereleases.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *BundleReleasesQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BundleReleasesSlice.
func (_q *BundleReleasesQuery) All(ctx context.Context) ([]*BundleReleases, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BundleReleases, *BundleReleasesQuery]()
	return withInterceptors[[]*BundleReleases](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *BundleReleasesQuery) AllX(ctx context.Context) []*BundleReleases {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BundleReleases IDs.
func (_q *BundleReleasesQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(bundlereleases.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *BundleReleasesQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *BundleReleasesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*BundleReleasesQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *BundleReleasesQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *BundleReleasesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *BundleReleasesQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BundleReleasesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *BundleReleasesQuery) Clone() *BundleReleasesQuery {
	if _q == nil {
		return nil
	}
	return &BundleReleasesQuery{
		config:                    _q.config,
		ctx:                       _q.ctx.Clone(),
		order:                     append([]bundlereleases.OrderOption{}, _q.order...),
		inters:                    append([]Interceptor{}, _q.inters...),
		predicates:                append([]predicate.BundleReleases{}, _q.predicates...),
		withBundle:                _q.withBundle.Clone(),
		withChannel:               _q.withChannel.Clone(),
		withCurrentForChannels:    _q.withCurrentForChannels.Clone(),
		withArtifacts:             _q.withArtifacts.Clone(),
		withBuildJobs:             _q.withBuildJobs.Clone(),
		withApprovals:             _q.withApprovals.Clone(),
		withOverrides:             _q.withOverrides.Clone(),
		withBaselineRollouts:      _q.withBaselineRollouts.Clone(),
		withCandidateRollouts:     _q.withCandidateRollouts.Clone(),
		withRolloutExposures:      _q.withRolloutExposures.Clone(),
		withControlExperiments:    _q.withControlExperiments.Clone(),
		withTreatmentExperiments:  _q.withTreatmentExperiments.Clone(),
		withReviewQueue:           _q.withReviewQueue.Clone(),
		withCrashEvents:           _q.withCrashEvents.Clone(),
		withBundleAbTestExposures: _q.withBundleAbTestExposures.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithBundle tells the query-builder to eager-load the nodes that are connected to
// the "bundle" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithBundle(opts ...func(*BundlesQuery)) *BundleReleasesQuery {
	query := (&BundlesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBundle = query
	return _q
}

// WithChannel tells the query-builder to eager-load the nodes that are connected to
// the "channel" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithChannel(opts ...func(*BundleChannelsQuery)) *BundleReleasesQuery {
	query := (&BundleChannelsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withChannel = query
	return _q
}

// WithCurrentForChannels tells the query-builder to eager-load the nodes that are connected to
// the "currentForChannels" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithCurrentForChannels(opts ...func(*BundleChannelsQuery)) *BundleReleasesQuery {
	query := (&BundleChannelsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCurrentForChannels = query
	return _q
}

// WithArtifacts tells the query-builder to eager-load the nodes that are connected to
// the "artifacts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithArtifacts(opts ...func(*BundleArtifactsQuery)) *BundleReleasesQuery {
	query := (&BundleArtifactsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withArtifacts = query
	return _q
}

// WithBuildJobs tells the query-builder to eager-load the nodes that are connected to
// the "buildJobs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithBuildJobs(opts ...func(*BundleBuildJobsQuery)) *BundleReleasesQuery {
	query := (&BundleBuildJobsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBuildJobs = query
	return _q
}

// WithApprovals tells the query-builder to eager-load the nodes that are connected to
// the "approvals" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithApprovals(opts ...func(*BundleReleaseApprovalsQuery)) *BundleReleasesQuery {
	query := (&BundleReleaseApprovalsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withApprovals = query
	return _q
}

// WithOverrides tells the query-builder to eager-load the nodes that are connected to
// the "overrides" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithOverrides(opts ...func(*BundleReleaseOverridesV2Query)) *BundleReleasesQuery {
	query := (&BundleReleaseOverridesV2Client{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOverrides = query
	return _q
}

// WithBaselineRollouts tells the query-builder to eager-load the nodes that are connected to
// the "baselineRollouts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithBaselineRollouts(opts ...func(*BundleDeliveryRolloutsQuery)) *BundleReleasesQuery {
	query := (&BundleDeliveryRolloutsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBaselineRollouts = query
	return _q
}

// WithCandidateRollouts tells the query-builder to eager-load the nodes that are connected to
// the "candidateRollouts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithCandidateRollouts(opts ...func(*BundleDeliveryRolloutsQuery)) *BundleReleasesQuery {
	query := (&BundleDeliveryRolloutsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCandidateRollouts = query
	return _q
}

// WithRolloutExposures tells the query-builder to eager-load the nodes that are connected to
// the "rolloutExposures" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithRolloutExposures(opts ...func(*BundleDeliveryRolloutExposuresQuery)) *BundleReleasesQuery {
	query := (&BundleDeliveryRolloutExposuresClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRolloutExposures = query
	return _q
}

// WithControlExperiments tells the query-builder to eager-load the nodes that are connected to
// the "controlExperiments" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithControlExperiments(opts ...func(*BundleAbTestsQuery)) *BundleReleasesQuery {
	query := (&BundleAbTestsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withControlExperiments = query
	return _q
}

// WithTreatmentExperiments tells the query-builder to eager-load the nodes that are connected to
// the "treatmentExperiments" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithTreatmentExperiments(opts ...func(*BundleAbTestsQuery)) *BundleReleasesQuery {
	query := (&BundleAbTestsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTreatmentExperiments = query
	return _q
}

// WithReviewQueue tells the query-builder to eager-load the nodes that are connected to
// the "reviewQueue" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithReviewQueue(opts ...func(*BundleReviewQueueQuery)) *BundleReleasesQuery {
	query := (&BundleReviewQueueClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withReviewQueue = query
	return _q
}

// WithCrashEvents tells the query-builder to eager-load the nodes that are connected to
// the "crashEvents" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithCrashEvents(opts ...func(*BundleCrashEventsQuery)) *BundleReleasesQuery {
	query := (&BundleCrashEventsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCrashEvents = query
	return _q
}

// WithBundleAbTestExposures tells the query-builder to eager-load the nodes that are connected to
// the "bundleAbTestExposures" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *BundleReleasesQuery) WithBundleAbTestExposures(opts ...func(*BundleAbTestExposuresQuery)) *BundleReleasesQuery {
	query := (&BundleAbTestExposuresClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBundleAbTestExposures = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BundleId uuid.UUID `json:"bundleId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BundleReleases.Query().
//		GroupBy(bundlereleases.FieldBundleId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *BundleReleasesQuery) GroupBy(field string, fields ...string) *BundleReleasesGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BundleReleasesGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = bundlereleases.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BundleId uuid.UUID `json:"bundleId,omitempty"`
//	}
//
//	client.BundleReleases.Query().
//		Select(bundlereleases.FieldBundleId).
//		Scan(ctx, &v)
func (_q *BundleReleasesQuery) Select(fields ...string) *BundleReleasesSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &BundleReleasesSelect{BundleReleasesQuery: _q}
	sbuild.label = bundlereleases.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BundleReleasesSelect configured with the given aggregations.
func (_q *BundleReleasesQuery) Aggregate(fns ...AggregateFunc) *BundleReleasesSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *BundleReleasesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !bundlereleases.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *BundleReleasesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BundleReleases, error) {
	var (
		nodes       = []*BundleReleases{}
		_spec       = _q.querySpec()
		loadedTypes = [15]bool{
			_q.withBundle != nil,
			_q.withChannel != nil,
			_q.withCurrentForChannels != nil,
			_q.withArtifacts != nil,
			_q.withBuildJobs != nil,
			_q.withApprovals != nil,
			_q.withOverrides != nil,
			_q.withBaselineRollouts != nil,
			_q.withCandidateRollouts != nil,
			_q.withRolloutExposures != nil,
			_q.withControlExperiments != nil,
			_q.withTreatmentExperiments != nil,
			_q.withReviewQueue != nil,
			_q.withCrashEvents != nil,
			_q.withBundleAbTestExposures != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BundleReleases).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BundleReleases{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withBundle; query != nil {
		if err := _q.loadBundle(ctx, query, nodes, nil,
			func(n *BundleReleases, e *Bundles) { n.Edges.Bundle = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withChannel; query != nil {
		if err := _q.loadChannel(ctx, query, nodes, nil,
			func(n *BundleReleases, e *BundleChannels) { n.Edges.Channel = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCurrentForChannels; query != nil {
		if err := _q.loadCurrentForChannels(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.CurrentForChannels = []*BundleChannels{} },
			func(n *BundleReleases, e *BundleChannels) {
				n.Edges.CurrentForChannels = append(n.Edges.CurrentForChannels, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withArtifacts; query != nil {
		if err := _q.loadArtifacts(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.Artifacts = []*BundleArtifacts{} },
			func(n *BundleReleases, e *BundleArtifacts) { n.Edges.Artifacts = append(n.Edges.Artifacts, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withBuildJobs; query != nil {
		if err := _q.loadBuildJobs(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.BuildJobs = []*BundleBuildJobs{} },
			func(n *BundleReleases, e *BundleBuildJobs) { n.Edges.BuildJobs = append(n.Edges.BuildJobs, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withApprovals; query != nil {
		if err := _q.loadApprovals(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.Approvals = []*BundleReleaseApprovals{} },
			func(n *BundleReleases, e *BundleReleaseApprovals) { n.Edges.Approvals = append(n.Edges.Approvals, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOverrides; query != nil {
		if err := _q.loadOverrides(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.Overrides = []*BundleReleaseOverridesV2{} },
			func(n *BundleReleases, e *BundleReleaseOverridesV2) { n.Edges.Overrides = append(n.Edges.Overrides, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withBaselineRollouts; query != nil {
		if err := _q.loadBaselineRollouts(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.BaselineRollouts = []*BundleDeliveryRollouts{} },
			func(n *BundleReleases, e *BundleDeliveryRollouts) {
				n.Edges.BaselineRollouts = append(n.Edges.BaselineRollouts, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withCandidateRollouts; query != nil {
		if err := _q.loadCandidateRollouts(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.CandidateRollouts = []*BundleDeliveryRollouts{} },
			func(n *BundleReleases, e *BundleDeliveryRollouts) {
				n.Edges.CandidateRollouts = append(n.Edges.CandidateRollouts, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withRolloutExposures; query != nil {
		if err := _q.loadRolloutExposures(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.RolloutExposures = []*BundleDeliveryRolloutExposures{} },
			func(n *BundleReleases, e *BundleDeliveryRolloutExposures) {
				n.Edges.RolloutExposures = append(n.Edges.RolloutExposures, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withControlExperiments; query != nil {
		if err := _q.loadControlExperiments(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.ControlExperiments = []*BundleAbTests{} },
			func(n *BundleReleases, e *BundleAbTests) {
				n.Edges.ControlExperiments = append(n.Edges.ControlExperiments, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withTreatmentExperiments; query != nil {
		if err := _q.loadTreatmentExperiments(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.TreatmentExperiments = []*BundleAbTests{} },
			func(n *BundleReleases, e *BundleAbTests) {
				n.Edges.TreatmentExperiments = append(n.Edges.TreatmentExperiments, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withReviewQueue; query != nil {
		if err := _q.loadReviewQueue(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.ReviewQueue = []*BundleReviewQueue{} },
			func(n *BundleReleases, e *BundleReviewQueue) { n.Edges.ReviewQueue = append(n.Edges.ReviewQueue, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCrashEvents; query != nil {
		if err := _q.loadCrashEvents(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.CrashEvents = []*BundleCrashEvents{} },
			func(n *BundleReleases, e *BundleCrashEvents) { n.Edges.CrashEvents = append(n.Edges.CrashEvents, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withBundleAbTestExposures; query != nil {
		if err := _q.loadBundleAbTestExposures(ctx, query, nodes,
			func(n *BundleReleases) { n.Edges.BundleAbTestExposures = []*BundleAbTestExposures{} },
			func(n *BundleReleases, e *BundleAbTestExposures) {
				n.Edges.BundleAbTestExposures = append(n.Edges.BundleAbTestExposures, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *BundleReleasesQuery) loadBundle(ctx context.Context, query *BundlesQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *Bundles)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleReleases)
	for i := range nodes {
		fk := nodes[i].BundleId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundles.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "bundleId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BundleReleasesQuery) loadChannel(ctx context.Context, query *BundleChannelsQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleChannels)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BundleReleases)
	for i := range nodes {
		fk := nodes[i].ChannelId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(bundlechannels.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "channelId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *BundleReleasesQuery) loadCurrentForChannels(ctx context.Context, query *BundleChannelsQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleChannels)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundlechannels.FieldCurrentReleaseId)
	}
	query.Where(predicate.BundleChannels(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.CurrentForChannelsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CurrentReleaseId
		if fk == nil {
			return fmt.Errorf(`foreign-key "currentReleaseId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "currentReleaseId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadArtifacts(ctx context.Context, query *BundleArtifactsQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleArtifacts)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundleartifacts.FieldReleaseId)
	}
	query.Where(predicate.BundleArtifacts(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.ArtifactsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ReleaseId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "releaseId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadBuildJobs(ctx context.Context, query *BundleBuildJobsQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleBuildJobs)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundlebuildjobs.FieldReleaseId)
	}
	query.Where(predicate.BundleBuildJobs(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.BuildJobsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ReleaseId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "releaseId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadApprovals(ctx context.Context, query *BundleReleaseApprovalsQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleReleaseApprovals)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundlereleaseapprovals.FieldReleaseId)
	}
	query.Where(predicate.BundleReleaseApprovals(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.ApprovalsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ReleaseId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "releaseId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadOverrides(ctx context.Context, query *BundleReleaseOverridesV2Query, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleReleaseOverridesV2)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundlereleaseoverridesv2.FieldReleaseId)
	}
	query.Where(predicate.BundleReleaseOverridesV2(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.OverridesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ReleaseId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "releaseId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadBaselineRollouts(ctx context.Context, query *BundleDeliveryRolloutsQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleDeliveryRollouts)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundledeliveryrollouts.FieldBaselineReleaseId)
	}
	query.Where(predicate.BundleDeliveryRollouts(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.BaselineRolloutsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.BaselineReleaseId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "baselineReleaseId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadCandidateRollouts(ctx context.Context, query *BundleDeliveryRolloutsQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleDeliveryRollouts)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundledeliveryrollouts.FieldCandidateReleaseId)
	}
	query.Where(predicate.BundleDeliveryRollouts(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.CandidateRolloutsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CandidateReleaseId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "candidateReleaseId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadRolloutExposures(ctx context.Context, query *BundleDeliveryRolloutExposuresQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleDeliveryRolloutExposures)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundledeliveryrolloutexposures.FieldReleaseId)
	}
	query.Where(predicate.BundleDeliveryRolloutExposures(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.RolloutExposuresColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ReleaseId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "releaseId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadControlExperiments(ctx context.Context, query *BundleAbTestsQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleAbTests)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundleabtests.FieldControlReleaseId)
	}
	query.Where(predicate.BundleAbTests(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.ControlExperimentsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ControlReleaseId
		if fk == nil {
			return fmt.Errorf(`foreign-key "controlReleaseId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "controlReleaseId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadTreatmentExperiments(ctx context.Context, query *BundleAbTestsQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleAbTests)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundleabtests.FieldTreatmentReleaseId)
	}
	query.Where(predicate.BundleAbTests(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.TreatmentExperimentsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.TreatmentReleaseId
		if fk == nil {
			return fmt.Errorf(`foreign-key "treatmentReleaseId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "treatmentReleaseId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadReviewQueue(ctx context.Context, query *BundleReviewQueueQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleReviewQueue)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundlereviewqueue.FieldReleaseId)
	}
	query.Where(predicate.BundleReviewQueue(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.ReviewQueueColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ReleaseId
		if fk == nil {
			return fmt.Errorf(`foreign-key "releaseId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "releaseId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadCrashEvents(ctx context.Context, query *BundleCrashEventsQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleCrashEvents)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundlecrashevents.FieldReleaseId)
	}
	query.Where(predicate.BundleCrashEvents(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.CrashEventsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ReleaseId
		if fk == nil {
			return fmt.Errorf(`foreign-key "releaseId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "releaseId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *BundleReleasesQuery) loadBundleAbTestExposures(ctx context.Context, query *BundleAbTestExposuresQuery, nodes []*BundleReleases, init func(*BundleReleases), assign func(*BundleReleases, *BundleAbTestExposures)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*BundleReleases)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(bundleabtestexposures.FieldAssignedReleaseId)
	}
	query.Where(predicate.BundleAbTestExposures(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(bundlereleases.BundleAbTestExposuresColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AssignedReleaseId
		if fk == nil {
			return fmt.Errorf(`foreign-key "assignedReleaseId" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "assignedReleaseId" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *BundleReleasesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *BundleReleasesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(bundlereleases.Table, bundlereleases.Columns, sqlgraph.NewFieldSpec(bundlereleases.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bundlereleases.FieldID)
		for i := range fields {
			if fields[i] != bundlereleases.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withBundle != nil {
			_spec.Node.AddColumnOnce(bundlereleases.FieldBundleId)
		}
		if _q.withChannel != nil {
			_spec.Node.AddColumnOnce(bundlereleases.FieldChannelId)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *BundleReleasesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(bundlereleases.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = bundlereleases.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *BundleReleasesQuery) ForUpdate(opts ...sql.LockOption) *BundleReleasesQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *BundleReleasesQuery) ForShare(opts ...sql.LockOption) *BundleReleasesQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *BundleReleasesQuery) Modify(modifiers ...func(s *sql.Selector)) *BundleReleasesSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// BundleReleasesGroupBy is the group-by builder for BundleReleases entities.
type BundleReleasesGroupBy struct {
	selector
	build *BundleReleasesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *BundleReleasesGroupBy) Aggregate(fns ...AggregateFunc) *BundleReleasesGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *BundleReleasesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BundleReleasesQuery, *BundleReleasesGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *BundleReleasesGroupBy) sqlScan(ctx context.Context, root *BundleReleasesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BundleReleasesSelect is the builder for selecting fields of BundleReleases entities.
type BundleReleasesSelect struct {
	*BundleReleasesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *BundleReleasesSelect) Aggregate(fns ...AggregateFunc) *BundleReleasesSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *BundleReleasesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BundleReleasesQuery, *BundleReleasesSelect](ctx, _s.BundleReleasesQuery, _s, _s.inters, v)
}

func (_s *BundleReleasesSelect) sqlScan(ctx context.Context, root *BundleReleasesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *BundleReleasesSelect) Modify(modifiers ...func(s *sql.Selector)) *BundleReleasesSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
