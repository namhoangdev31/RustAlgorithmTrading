// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"trading/control-gateway/internal/data/ent/bundles"
	"trading/control-gateway/internal/data/ent/bundletags"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// BundleTags is the model entity for the BundleTags schema.
type BundleTags struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// BundleId holds the value of the "bundleId" field.
	BundleId uuid.UUID `json:"bundleId,omitempty"`
	// Tag holds the value of the "tag" field.
	Tag string `json:"tag,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BundleTagsQuery when eager-loading is set.
	Edges        BundleTagsEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BundleTagsEdges holds the relations/edges for other nodes in the graph.
type BundleTagsEdges struct {
	// Bundle holds the value of the bundle edge.
	Bundle *Bundles `json:"bundle,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BundleOrErr returns the Bundle value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BundleTagsEdges) BundleOrErr() (*Bundles, error) {
	if e.Bundle != nil {
		return e.Bundle, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: bundles.Label}
	}
	return nil, &NotLoadedError{edge: "bundle"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BundleTags) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bundletags.FieldTag:
			values[i] = new(sql.NullString)
		case bundletags.FieldID, bundletags.FieldBundleId:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BundleTags fields.
func (_m *BundleTags) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bundletags.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case bundletags.FieldBundleId:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field bundleId", values[i])
			} else if value != nil {
				_m.BundleId = *value
			}
		case bundletags.FieldTag:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tag", values[i])
			} else if value.Valid {
				_m.Tag = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BundleTags.
// This includes values selected through modifiers, order, etc.
func (_m *BundleTags) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBundle queries the "bundle" edge of the BundleTags entity.
func (_m *BundleTags) QueryBundle() *BundlesQuery {
	return NewBundleTagsClient(_m.config).QueryBundle(_m)
}

// Update returns a builder for updating this BundleTags.
// Note that you need to call BundleTags.Unwrap() before calling this method if this BundleTags
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BundleTags) Update() *BundleTagsUpdateOne {
	return NewBundleTagsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BundleTags entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BundleTags) Unwrap() *BundleTags {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BundleTags is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BundleTags) String() string {
	var builder strings.Builder
	builder.WriteString("BundleTags(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("bundleId=")
	builder.WriteString(fmt.Sprintf("%v", _m.BundleId))
	builder.WriteString(", ")
	builder.WriteString("tag=")
	builder.WriteString(_m.Tag)
	builder.WriteByte(')')
	return builder.String()
}

// BundleTagsSlice is a parsable slice of BundleTags.
type BundleTagsSlice []*BundleTags
