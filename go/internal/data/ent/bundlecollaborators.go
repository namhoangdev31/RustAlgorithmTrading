// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"trading/control-gateway/internal/data/ent/bundlecollaborators"
	"trading/control-gateway/internal/data/ent/bundles"
	"trading/control-gateway/internal/data/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/lib/pq"
)

// BundleCollaborators is the model entity for the BundleCollaborators schema.
type BundleCollaborators struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// BundleId holds the value of the "bundleId" field.
	BundleId uuid.UUID `json:"bundleId,omitempty"`
	// UserId holds the value of the "userId" field.
	UserId uuid.UUID `json:"userId,omitempty"`
	// Role holds the value of the "role" field.
	Role string `json:"role,omitempty"`
	// PermissionKeys holds the value of the "permissionKeys" field.
	PermissionKeys pq.StringArray `json:"permissionKeys,omitempty"`
	// InvitedBy holds the value of the "invitedBy" field.
	InvitedBy *uuid.UUID `json:"invitedBy,omitempty"`
	// AcceptedAt holds the value of the "acceptedAt" field.
	AcceptedAt *time.Time `json:"acceptedAt,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BundleCollaboratorsQuery when eager-loading is set.
	Edges        BundleCollaboratorsEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BundleCollaboratorsEdges holds the relations/edges for other nodes in the graph.
type BundleCollaboratorsEdges struct {
	// Bundle holds the value of the bundle edge.
	Bundle *Bundles `json:"bundle,omitempty"`
	// InvitedUser holds the value of the invitedUser edge.
	InvitedUser *User `json:"invitedUser,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// BundleOrErr returns the Bundle value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BundleCollaboratorsEdges) BundleOrErr() (*Bundles, error) {
	if e.Bundle != nil {
		return e.Bundle, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: bundles.Label}
	}
	return nil, &NotLoadedError{edge: "bundle"}
}

// InvitedUserOrErr returns the InvitedUser value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BundleCollaboratorsEdges) InvitedUserOrErr() (*User, error) {
	if e.InvitedUser != nil {
		return e.InvitedUser, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "invitedUser"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BundleCollaboratorsEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BundleCollaborators) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bundlecollaborators.FieldInvitedBy:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case bundlecollaborators.FieldPermissionKeys:
			values[i] = new(pq.StringArray)
		case bundlecollaborators.FieldRole:
			values[i] = new(sql.NullString)
		case bundlecollaborators.FieldAcceptedAt, bundlecollaborators.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case bundlecollaborators.FieldID, bundlecollaborators.FieldBundleId, bundlecollaborators.FieldUserId:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BundleCollaborators fields.
func (_m *BundleCollaborators) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bundlecollaborators.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case bundlecollaborators.FieldBundleId:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field bundleId", values[i])
			} else if value != nil {
				_m.BundleId = *value
			}
		case bundlecollaborators.FieldUserId:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field userId", values[i])
			} else if value != nil {
				_m.UserId = *value
			}
		case bundlecollaborators.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				_m.Role = value.String
			}
		case bundlecollaborators.FieldPermissionKeys:
			if value, ok := values[i].(*pq.StringArray); !ok {
				return fmt.Errorf("unexpected type %T for field permissionKeys", values[i])
			} else if value != nil {
				_m.PermissionKeys = *value
			}
		case bundlecollaborators.FieldInvitedBy:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field invitedBy", values[i])
			} else if value.Valid {
				_m.InvitedBy = new(uuid.UUID)
				*_m.InvitedBy = *value.S.(*uuid.UUID)
			}
		case bundlecollaborators.FieldAcceptedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field acceptedAt", values[i])
			} else if value.Valid {
				_m.AcceptedAt = new(time.Time)
				*_m.AcceptedAt = value.Time
			}
		case bundlecollaborators.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BundleCollaborators.
// This includes values selected through modifiers, order, etc.
func (_m *BundleCollaborators) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBundle queries the "bundle" edge of the BundleCollaborators entity.
func (_m *BundleCollaborators) QueryBundle() *BundlesQuery {
	return NewBundleCollaboratorsClient(_m.config).QueryBundle(_m)
}

// QueryInvitedUser queries the "invitedUser" edge of the BundleCollaborators entity.
func (_m *BundleCollaborators) QueryInvitedUser() *UserQuery {
	return NewBundleCollaboratorsClient(_m.config).QueryInvitedUser(_m)
}

// QueryUser queries the "user" edge of the BundleCollaborators entity.
func (_m *BundleCollaborators) QueryUser() *UserQuery {
	return NewBundleCollaboratorsClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this BundleCollaborators.
// Note that you need to call BundleCollaborators.Unwrap() before calling this method if this BundleCollaborators
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BundleCollaborators) Update() *BundleCollaboratorsUpdateOne {
	return NewBundleCollaboratorsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BundleCollaborators entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BundleCollaborators) Unwrap() *BundleCollaborators {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BundleCollaborators is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BundleCollaborators) String() string {
	var builder strings.Builder
	builder.WriteString("BundleCollaborators(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("bundleId=")
	builder.WriteString(fmt.Sprintf("%v", _m.BundleId))
	builder.WriteString(", ")
	builder.WriteString("userId=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserId))
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(_m.Role)
	builder.WriteString(", ")
	builder.WriteString("permissionKeys=")
	builder.WriteString(fmt.Sprintf("%v", _m.PermissionKeys))
	builder.WriteString(", ")
	if v := _m.InvitedBy; v != nil {
		builder.WriteString("invitedBy=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := _m.AcceptedAt; v != nil {
		builder.WriteString("acceptedAt=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// BundleCollaboratorsSlice is a parsable slice of BundleCollaborators.
type BundleCollaboratorsSlice []*BundleCollaborators
