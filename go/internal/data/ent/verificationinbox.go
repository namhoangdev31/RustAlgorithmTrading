// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"trading/control-gateway/internal/data/ent/verificationinbox"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// VerificationInbox is the model entity for the VerificationInbox schema.
type VerificationInbox struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Consumer holds the value of the "consumer" field.
	Consumer string `json:"consumer,omitempty"`
	// MessageId holds the value of the "messageId" field.
	MessageId string `json:"messageId,omitempty"`
	// Outcome holds the value of the "outcome" field.
	Outcome *string `json:"outcome,omitempty"`
	// ProcessedAt holds the value of the "processedAt" field.
	ProcessedAt  time.Time `json:"processedAt,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VerificationInbox) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case verificationinbox.FieldConsumer, verificationinbox.FieldMessageId, verificationinbox.FieldOutcome:
			values[i] = new(sql.NullString)
		case verificationinbox.FieldProcessedAt:
			values[i] = new(sql.NullTime)
		case verificationinbox.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VerificationInbox fields.
func (_m *VerificationInbox) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case verificationinbox.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case verificationinbox.FieldConsumer:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field consumer", values[i])
			} else if value.Valid {
				_m.Consumer = value.String
			}
		case verificationinbox.FieldMessageId:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field messageId", values[i])
			} else if value.Valid {
				_m.MessageId = value.String
			}
		case verificationinbox.FieldOutcome:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field outcome", values[i])
			} else if value.Valid {
				_m.Outcome = new(string)
				*_m.Outcome = value.String
			}
		case verificationinbox.FieldProcessedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field processedAt", values[i])
			} else if value.Valid {
				_m.ProcessedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the VerificationInbox.
// This includes values selected through modifiers, order, etc.
func (_m *VerificationInbox) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this VerificationInbox.
// Note that you need to call VerificationInbox.Unwrap() before calling this method if this VerificationInbox
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *VerificationInbox) Update() *VerificationInboxUpdateOne {
	return NewVerificationInboxClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the VerificationInbox entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *VerificationInbox) Unwrap() *VerificationInbox {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: VerificationInbox is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *VerificationInbox) String() string {
	var builder strings.Builder
	builder.WriteString("VerificationInbox(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("consumer=")
	builder.WriteString(_m.Consumer)
	builder.WriteString(", ")
	builder.WriteString("messageId=")
	builder.WriteString(_m.MessageId)
	builder.WriteString(", ")
	if v := _m.Outcome; v != nil {
		builder.WriteString("outcome=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("processedAt=")
	builder.WriteString(_m.ProcessedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// VerificationInboxes is a parsable slice of VerificationInbox.
type VerificationInboxes []*VerificationInbox
