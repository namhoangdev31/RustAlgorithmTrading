// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"trading/control-gateway/internal/data/ent/organization"
	"trading/control-gateway/internal/data/ent/predicate"
	"trading/control-gateway/internal/data/ent/projectproviderbinding"
	"trading/control-gateway/internal/data/ent/workspaceproviderconnection"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// WorkspaceProviderConnectionQuery is the builder for querying WorkspaceProviderConnection entities.
type WorkspaceProviderConnectionQuery struct {
	config
	ctx                 *QueryContext
	order               []workspaceproviderconnection.OrderOption
	inters              []Interceptor
	predicates          []predicate.WorkspaceProviderConnection
	withOrganization    *OrganizationQuery
	withProjectBindings *ProjectProviderBindingQuery
	modifiers           []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WorkspaceProviderConnectionQuery builder.
func (_q *WorkspaceProviderConnectionQuery) Where(ps ...predicate.WorkspaceProviderConnection) *WorkspaceProviderConnectionQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *WorkspaceProviderConnectionQuery) Limit(limit int) *WorkspaceProviderConnectionQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *WorkspaceProviderConnectionQuery) Offset(offset int) *WorkspaceProviderConnectionQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *WorkspaceProviderConnectionQuery) Unique(unique bool) *WorkspaceProviderConnectionQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *WorkspaceProviderConnectionQuery) Order(o ...workspaceproviderconnection.OrderOption) *WorkspaceProviderConnectionQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryOrganization chains the current query on the "organization" edge.
func (_q *WorkspaceProviderConnectionQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workspaceproviderconnection.Table, workspaceproviderconnection.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, workspaceproviderconnection.OrganizationTable, workspaceproviderconnection.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProjectBindings chains the current query on the "projectBindings" edge.
func (_q *WorkspaceProviderConnectionQuery) QueryProjectBindings() *ProjectProviderBindingQuery {
	query := (&ProjectProviderBindingClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(workspaceproviderconnection.Table, workspaceproviderconnection.FieldID, selector),
			sqlgraph.To(projectproviderbinding.Table, projectproviderbinding.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, workspaceproviderconnection.ProjectBindingsTable, workspaceproviderconnection.ProjectBindingsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first WorkspaceProviderConnection entity from the query.
// Returns a *NotFoundError when no WorkspaceProviderConnection was found.
func (_q *WorkspaceProviderConnectionQuery) First(ctx context.Context) (*WorkspaceProviderConnection, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{workspaceproviderconnection.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *WorkspaceProviderConnectionQuery) FirstX(ctx context.Context) *WorkspaceProviderConnection {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WorkspaceProviderConnection ID from the query.
// Returns a *NotFoundError when no WorkspaceProviderConnection ID was found.
func (_q *WorkspaceProviderConnectionQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{workspaceproviderconnection.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *WorkspaceProviderConnectionQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WorkspaceProviderConnection entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WorkspaceProviderConnection entity is found.
// Returns a *NotFoundError when no WorkspaceProviderConnection entities are found.
func (_q *WorkspaceProviderConnectionQuery) Only(ctx context.Context) (*WorkspaceProviderConnection, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{workspaceproviderconnection.Label}
	default:
		return nil, &NotSingularError{workspaceproviderconnection.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *WorkspaceProviderConnectionQuery) OnlyX(ctx context.Context) *WorkspaceProviderConnection {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WorkspaceProviderConnection ID in the query.
// Returns a *NotSingularError when more than one WorkspaceProviderConnection ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *WorkspaceProviderConnectionQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{workspaceproviderconnection.Label}
	default:
		err = &NotSingularError{workspaceproviderconnection.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *WorkspaceProviderConnectionQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WorkspaceProviderConnections.
func (_q *WorkspaceProviderConnectionQuery) All(ctx context.Context) ([]*WorkspaceProviderConnection, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WorkspaceProviderConnection, *WorkspaceProviderConnectionQuery]()
	return withInterceptors[[]*WorkspaceProviderConnection](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *WorkspaceProviderConnectionQuery) AllX(ctx context.Context) []*WorkspaceProviderConnection {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WorkspaceProviderConnection IDs.
func (_q *WorkspaceProviderConnectionQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(workspaceproviderconnection.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *WorkspaceProviderConnectionQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *WorkspaceProviderConnectionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*WorkspaceProviderConnectionQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *WorkspaceProviderConnectionQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *WorkspaceProviderConnectionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *WorkspaceProviderConnectionQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WorkspaceProviderConnectionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *WorkspaceProviderConnectionQuery) Clone() *WorkspaceProviderConnectionQuery {
	if _q == nil {
		return nil
	}
	return &WorkspaceProviderConnectionQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]workspaceproviderconnection.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.WorkspaceProviderConnection{}, _q.predicates...),
		withOrganization:    _q.withOrganization.Clone(),
		withProjectBindings: _q.withProjectBindings.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkspaceProviderConnectionQuery) WithOrganization(opts ...func(*OrganizationQuery)) *WorkspaceProviderConnectionQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrganization = query
	return _q
}

// WithProjectBindings tells the query-builder to eager-load the nodes that are connected to
// the "projectBindings" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *WorkspaceProviderConnectionQuery) WithProjectBindings(opts ...func(*ProjectProviderBindingQuery)) *WorkspaceProviderConnectionQuery {
	query := (&ProjectProviderBindingClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withProjectBindings = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		OrganizationId uuid.UUID `json:"organizationId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WorkspaceProviderConnection.Query().
//		GroupBy(workspaceproviderconnection.FieldOrganizationId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *WorkspaceProviderConnectionQuery) GroupBy(field string, fields ...string) *WorkspaceProviderConnectionGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WorkspaceProviderConnectionGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = workspaceproviderconnection.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		OrganizationId uuid.UUID `json:"organizationId,omitempty"`
//	}
//
//	client.WorkspaceProviderConnection.Query().
//		Select(workspaceproviderconnection.FieldOrganizationId).
//		Scan(ctx, &v)
func (_q *WorkspaceProviderConnectionQuery) Select(fields ...string) *WorkspaceProviderConnectionSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &WorkspaceProviderConnectionSelect{WorkspaceProviderConnectionQuery: _q}
	sbuild.label = workspaceproviderconnection.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WorkspaceProviderConnectionSelect configured with the given aggregations.
func (_q *WorkspaceProviderConnectionQuery) Aggregate(fns ...AggregateFunc) *WorkspaceProviderConnectionSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *WorkspaceProviderConnectionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !workspaceproviderconnection.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *WorkspaceProviderConnectionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WorkspaceProviderConnection, error) {
	var (
		nodes       = []*WorkspaceProviderConnection{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withOrganization != nil,
			_q.withProjectBindings != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WorkspaceProviderConnection).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WorkspaceProviderConnection{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withOrganization; query != nil {
		if err := _q.loadOrganization(ctx, query, nodes, nil,
			func(n *WorkspaceProviderConnection, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withProjectBindings; query != nil {
		if err := _q.loadProjectBindings(ctx, query, nodes,
			func(n *WorkspaceProviderConnection) { n.Edges.ProjectBindings = []*ProjectProviderBinding{} },
			func(n *WorkspaceProviderConnection, e *ProjectProviderBinding) {
				n.Edges.ProjectBindings = append(n.Edges.ProjectBindings, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *WorkspaceProviderConnectionQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*WorkspaceProviderConnection, init func(*WorkspaceProviderConnection), assign func(*WorkspaceProviderConnection, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*WorkspaceProviderConnection)
	for i := range nodes {
		fk := nodes[i].OrganizationId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organizationId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *WorkspaceProviderConnectionQuery) loadProjectBindings(ctx context.Context, query *ProjectProviderBindingQuery, nodes []*WorkspaceProviderConnection, init func(*WorkspaceProviderConnection), assign func(*WorkspaceProviderConnection, *ProjectProviderBinding)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*WorkspaceProviderConnection)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(projectproviderbinding.FieldConnectionId)
	}
	query.Where(predicate.ProjectProviderBinding(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(workspaceproviderconnection.ProjectBindingsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ConnectionId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "connectionId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *WorkspaceProviderConnectionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *WorkspaceProviderConnectionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(workspaceproviderconnection.Table, workspaceproviderconnection.Columns, sqlgraph.NewFieldSpec(workspaceproviderconnection.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, workspaceproviderconnection.FieldID)
		for i := range fields {
			if fields[i] != workspaceproviderconnection.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withOrganization != nil {
			_spec.Node.AddColumnOnce(workspaceproviderconnection.FieldOrganizationId)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *WorkspaceProviderConnectionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(workspaceproviderconnection.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = workspaceproviderconnection.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *WorkspaceProviderConnectionQuery) ForUpdate(opts ...sql.LockOption) *WorkspaceProviderConnectionQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *WorkspaceProviderConnectionQuery) ForShare(opts ...sql.LockOption) *WorkspaceProviderConnectionQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *WorkspaceProviderConnectionQuery) Modify(modifiers ...func(s *sql.Selector)) *WorkspaceProviderConnectionSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// WorkspaceProviderConnectionGroupBy is the group-by builder for WorkspaceProviderConnection entities.
type WorkspaceProviderConnectionGroupBy struct {
	selector
	build *WorkspaceProviderConnectionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *WorkspaceProviderConnectionGroupBy) Aggregate(fns ...AggregateFunc) *WorkspaceProviderConnectionGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *WorkspaceProviderConnectionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkspaceProviderConnectionQuery, *WorkspaceProviderConnectionGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *WorkspaceProviderConnectionGroupBy) sqlScan(ctx context.Context, root *WorkspaceProviderConnectionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WorkspaceProviderConnectionSelect is the builder for selecting fields of WorkspaceProviderConnection entities.
type WorkspaceProviderConnectionSelect struct {
	*WorkspaceProviderConnectionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *WorkspaceProviderConnectionSelect) Aggregate(fns ...AggregateFunc) *WorkspaceProviderConnectionSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *WorkspaceProviderConnectionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WorkspaceProviderConnectionQuery, *WorkspaceProviderConnectionSelect](ctx, _s.WorkspaceProviderConnectionQuery, _s, _s.inters, v)
}

func (_s *WorkspaceProviderConnectionSelect) sqlScan(ctx context.Context, root *WorkspaceProviderConnectionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *WorkspaceProviderConnectionSelect) Modify(modifiers ...func(s *sql.Selector)) *WorkspaceProviderConnectionSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
