// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"trading/control-gateway/internal/data/ent/form"
	"trading/control-gateway/internal/data/ent/formsubmission"
	"trading/control-gateway/internal/data/ent/formwebhookdelivery"
	"trading/control-gateway/internal/data/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FormSubmissionQuery is the builder for querying FormSubmission entities.
type FormSubmissionQuery struct {
	config
	ctx                   *QueryContext
	order                 []formsubmission.OrderOption
	inters                []Interceptor
	predicates            []predicate.FormSubmission
	withForm              *FormQuery
	withWebhookDeliveries *FormWebhookDeliveryQuery
	modifiers             []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FormSubmissionQuery builder.
func (_q *FormSubmissionQuery) Where(ps ...predicate.FormSubmission) *FormSubmissionQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *FormSubmissionQuery) Limit(limit int) *FormSubmissionQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *FormSubmissionQuery) Offset(offset int) *FormSubmissionQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *FormSubmissionQuery) Unique(unique bool) *FormSubmissionQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *FormSubmissionQuery) Order(o ...formsubmission.OrderOption) *FormSubmissionQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryForm chains the current query on the "form" edge.
func (_q *FormSubmissionQuery) QueryForm() *FormQuery {
	query := (&FormClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(formsubmission.Table, formsubmission.FieldID, selector),
			sqlgraph.To(form.Table, form.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, formsubmission.FormTable, formsubmission.FormColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryWebhookDeliveries chains the current query on the "webhookDeliveries" edge.
func (_q *FormSubmissionQuery) QueryWebhookDeliveries() *FormWebhookDeliveryQuery {
	query := (&FormWebhookDeliveryClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(formsubmission.Table, formsubmission.FieldID, selector),
			sqlgraph.To(formwebhookdelivery.Table, formwebhookdelivery.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, formsubmission.WebhookDeliveriesTable, formsubmission.WebhookDeliveriesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FormSubmission entity from the query.
// Returns a *NotFoundError when no FormSubmission was found.
func (_q *FormSubmissionQuery) First(ctx context.Context) (*FormSubmission, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{formsubmission.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *FormSubmissionQuery) FirstX(ctx context.Context) *FormSubmission {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FormSubmission ID from the query.
// Returns a *NotFoundError when no FormSubmission ID was found.
func (_q *FormSubmissionQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{formsubmission.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *FormSubmissionQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FormSubmission entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FormSubmission entity is found.
// Returns a *NotFoundError when no FormSubmission entities are found.
func (_q *FormSubmissionQuery) Only(ctx context.Context) (*FormSubmission, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{formsubmission.Label}
	default:
		return nil, &NotSingularError{formsubmission.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *FormSubmissionQuery) OnlyX(ctx context.Context) *FormSubmission {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FormSubmission ID in the query.
// Returns a *NotSingularError when more than one FormSubmission ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *FormSubmissionQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{formsubmission.Label}
	default:
		err = &NotSingularError{formsubmission.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *FormSubmissionQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FormSubmissions.
func (_q *FormSubmissionQuery) All(ctx context.Context) ([]*FormSubmission, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FormSubmission, *FormSubmissionQuery]()
	return withInterceptors[[]*FormSubmission](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *FormSubmissionQuery) AllX(ctx context.Context) []*FormSubmission {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FormSubmission IDs.
func (_q *FormSubmissionQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(formsubmission.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *FormSubmissionQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *FormSubmissionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*FormSubmissionQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *FormSubmissionQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *FormSubmissionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *FormSubmissionQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FormSubmissionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *FormSubmissionQuery) Clone() *FormSubmissionQuery {
	if _q == nil {
		return nil
	}
	return &FormSubmissionQuery{
		config:                _q.config,
		ctx:                   _q.ctx.Clone(),
		order:                 append([]formsubmission.OrderOption{}, _q.order...),
		inters:                append([]Interceptor{}, _q.inters...),
		predicates:            append([]predicate.FormSubmission{}, _q.predicates...),
		withForm:              _q.withForm.Clone(),
		withWebhookDeliveries: _q.withWebhookDeliveries.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithForm tells the query-builder to eager-load the nodes that are connected to
// the "form" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FormSubmissionQuery) WithForm(opts ...func(*FormQuery)) *FormSubmissionQuery {
	query := (&FormClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withForm = query
	return _q
}

// WithWebhookDeliveries tells the query-builder to eager-load the nodes that are connected to
// the "webhookDeliveries" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *FormSubmissionQuery) WithWebhookDeliveries(opts ...func(*FormWebhookDeliveryQuery)) *FormSubmissionQuery {
	query := (&FormWebhookDeliveryClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withWebhookDeliveries = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Data json.RawMessage `json:"data,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FormSubmission.Query().
//		GroupBy(formsubmission.FieldData).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *FormSubmissionQuery) GroupBy(field string, fields ...string) *FormSubmissionGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FormSubmissionGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = formsubmission.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Data json.RawMessage `json:"data,omitempty"`
//	}
//
//	client.FormSubmission.Query().
//		Select(formsubmission.FieldData).
//		Scan(ctx, &v)
func (_q *FormSubmissionQuery) Select(fields ...string) *FormSubmissionSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &FormSubmissionSelect{FormSubmissionQuery: _q}
	sbuild.label = formsubmission.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FormSubmissionSelect configured with the given aggregations.
func (_q *FormSubmissionQuery) Aggregate(fns ...AggregateFunc) *FormSubmissionSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *FormSubmissionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !formsubmission.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *FormSubmissionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FormSubmission, error) {
	var (
		nodes       = []*FormSubmission{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withForm != nil,
			_q.withWebhookDeliveries != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FormSubmission).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FormSubmission{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withForm; query != nil {
		if err := _q.loadForm(ctx, query, nodes, nil,
			func(n *FormSubmission, e *Form) { n.Edges.Form = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withWebhookDeliveries; query != nil {
		if err := _q.loadWebhookDeliveries(ctx, query, nodes,
			func(n *FormSubmission) { n.Edges.WebhookDeliveries = []*FormWebhookDelivery{} },
			func(n *FormSubmission, e *FormWebhookDelivery) {
				n.Edges.WebhookDeliveries = append(n.Edges.WebhookDeliveries, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *FormSubmissionQuery) loadForm(ctx context.Context, query *FormQuery, nodes []*FormSubmission, init func(*FormSubmission), assign func(*FormSubmission, *Form)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*FormSubmission)
	for i := range nodes {
		fk := nodes[i].FormId
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(form.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "formId" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *FormSubmissionQuery) loadWebhookDeliveries(ctx context.Context, query *FormWebhookDeliveryQuery, nodes []*FormSubmission, init func(*FormSubmission), assign func(*FormSubmission, *FormWebhookDelivery)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*FormSubmission)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(formwebhookdelivery.FieldSubmissionId)
	}
	query.Where(predicate.FormWebhookDelivery(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(formsubmission.WebhookDeliveriesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SubmissionId
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "submissionId" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *FormSubmissionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *FormSubmissionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(formsubmission.Table, formsubmission.Columns, sqlgraph.NewFieldSpec(formsubmission.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, formsubmission.FieldID)
		for i := range fields {
			if fields[i] != formsubmission.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withForm != nil {
			_spec.Node.AddColumnOnce(formsubmission.FieldFormId)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *FormSubmissionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(formsubmission.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = formsubmission.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *FormSubmissionQuery) ForUpdate(opts ...sql.LockOption) *FormSubmissionQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *FormSubmissionQuery) ForShare(opts ...sql.LockOption) *FormSubmissionQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *FormSubmissionQuery) Modify(modifiers ...func(s *sql.Selector)) *FormSubmissionSelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// FormSubmissionGroupBy is the group-by builder for FormSubmission entities.
type FormSubmissionGroupBy struct {
	selector
	build *FormSubmissionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *FormSubmissionGroupBy) Aggregate(fns ...AggregateFunc) *FormSubmissionGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *FormSubmissionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FormSubmissionQuery, *FormSubmissionGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *FormSubmissionGroupBy) sqlScan(ctx context.Context, root *FormSubmissionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FormSubmissionSelect is the builder for selecting fields of FormSubmission entities.
type FormSubmissionSelect struct {
	*FormSubmissionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *FormSubmissionSelect) Aggregate(fns ...AggregateFunc) *FormSubmissionSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *FormSubmissionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FormSubmissionQuery, *FormSubmissionSelect](ctx, _s.FormSubmissionQuery, _s, _s.inters, v)
}

func (_s *FormSubmissionSelect) sqlScan(ctx context.Context, root *FormSubmissionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *FormSubmissionSelect) Modify(modifiers ...func(s *sql.Selector)) *FormSubmissionSelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
