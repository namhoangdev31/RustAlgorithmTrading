// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"trading/control-gateway/internal/data/ent/user"
	"trading/control-gateway/internal/data/ent/usersecrets"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// UserSecrets is the model entity for the UserSecrets schema.
type UserSecrets struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// UserId holds the value of the "userId" field.
	UserId uuid.UUID `json:"userId,omitempty"`
	// Provider holds the value of the "provider" field.
	Provider string `json:"provider,omitempty"`
	// EncryptedValue holds the value of the "encryptedValue" field.
	EncryptedValue string `json:"encryptedValue,omitempty"`
	// CreatedAt holds the value of the "createdAt" field.
	CreatedAt time.Time `json:"createdAt,omitempty"`
	// UpdatedAt holds the value of the "updatedAt" field.
	UpdatedAt time.Time `json:"updatedAt,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserSecretsQuery when eager-loading is set.
	Edges        UserSecretsEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserSecretsEdges holds the relations/edges for other nodes in the graph.
type UserSecretsEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserSecretsEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserSecrets) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case usersecrets.FieldProvider, usersecrets.FieldEncryptedValue:
			values[i] = new(sql.NullString)
		case usersecrets.FieldCreatedAt, usersecrets.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case usersecrets.FieldID, usersecrets.FieldUserId:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserSecrets fields.
func (_m *UserSecrets) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case usersecrets.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case usersecrets.FieldUserId:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field userId", values[i])
			} else if value != nil {
				_m.UserId = *value
			}
		case usersecrets.FieldProvider:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[i])
			} else if value.Valid {
				_m.Provider = value.String
			}
		case usersecrets.FieldEncryptedValue:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field encryptedValue", values[i])
			} else if value.Valid {
				_m.EncryptedValue = value.String
			}
		case usersecrets.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field createdAt", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case usersecrets.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updatedAt", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserSecrets.
// This includes values selected through modifiers, order, etc.
func (_m *UserSecrets) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserSecrets entity.
func (_m *UserSecrets) QueryUser() *UserQuery {
	return NewUserSecretsClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this UserSecrets.
// Note that you need to call UserSecrets.Unwrap() before calling this method if this UserSecrets
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserSecrets) Update() *UserSecretsUpdateOne {
	return NewUserSecretsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserSecrets entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserSecrets) Unwrap() *UserSecrets {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserSecrets is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserSecrets) String() string {
	var builder strings.Builder
	builder.WriteString("UserSecrets(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("userId=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserId))
	builder.WriteString(", ")
	builder.WriteString("provider=")
	builder.WriteString(_m.Provider)
	builder.WriteString(", ")
	builder.WriteString("encryptedValue=")
	builder.WriteString(_m.EncryptedValue)
	builder.WriteString(", ")
	builder.WriteString("createdAt=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updatedAt=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserSecretsSlice is a parsable slice of UserSecrets.
type UserSecretsSlice []*UserSecrets
